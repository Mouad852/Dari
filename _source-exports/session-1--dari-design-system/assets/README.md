No logo, photography, illustration or icon binaries were provided with the brief, so this folder is intentionally empty.

- Wordmark: set in type (Plus Jakarta Sans 800, lowercase, tracking -.02em). See `guidelines/brand-wordmark.card.html`.
- Icons: Lucide, loaded from CDN by `components/core/Icon.jsx`.
- Photography: image slots render a `--sable-200` placeholder labelled PHOTO.

Drop real files here (logo.svg, photos, illustrations) and update `readme.md` → Assets.

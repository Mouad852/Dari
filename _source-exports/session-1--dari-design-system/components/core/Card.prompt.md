Every panel in Dari is a Card: white, 18px radius, hairline border, warm shadow.

```jsx
<Card interactive elevation="sm"><h3>Studio à Agdal</h3></Card>
```
Use `padding="0"` when the card starts with a full-bleed image.

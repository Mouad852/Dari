Circular icon control. Use `glass` when it sits on a listing photo.

```jsx
<IconButton icon="heart" variant="glass" active label="Enregistrer" />
<IconButton icon="chevron-left" label="Retour" />
```

One of a set — always give the group a heading above it.

```jsx
<Radio name="type" label="Chambre en colocation" description="Espaces communs partagés" checked />
<Radio name="type" label="Logement entier" />
```

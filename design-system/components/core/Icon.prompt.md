Lucide glyph as a currentColor mask — use for every icon in Dari UI; never hand-draw SVG.

```jsx
<Icon name="map-pin" size={16} />
<span style={{color:'var(--brand)'}}><Icon name="shield-check" size={20}/></span>
```

Dari's canonical slugs: `map-pin`, `heart`, `search`, `sliders-horizontal`, `shield-check`, `star`, `message-circle`, `user-round`, `calendar`, `bed-double`, `wifi`, `sparkles`, `chevron-right`, `x`. Sizes: 16 inline, 20 default, 24 nav.

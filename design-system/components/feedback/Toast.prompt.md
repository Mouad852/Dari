Short confirmation, 3s, floating 16px above the tab bar. Never for errors that need a decision — use Dialog.

```jsx
<Toast tone="success">Annonce enregistrée</Toast>
<Toast action="Annuler" onAction={undo}>Message envoyé à Salma</Toast>
```

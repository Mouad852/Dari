Standard text field — 44px, 12px radius, hairline border, terracotta focus ring.

```jsx
<Input label="Budget mensuel" suffix="MAD" placeholder="3 500" iconLeft="wallet" />
<Input label="Email" error="Cet email est déjà utilisé" />
```

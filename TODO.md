# Dari TODO

This is the single ongoing completion checklist for the project. Every coding session must update this file when work changes the project status. Check items only after verifying them against source, tests, or a production-like runtime.

Last verified: 2026-09-08

## Current baseline

- [x] Spring Boot API builds and runs against PostgreSQL 16 + PostGIS
- [x] Flyway migrations apply through `V21__listing_rooms.sql`
- [x] Backend suite: 127 tests, 0 failures, 0 errors, no known flakes remaining (2026-09-08) — verified with 8 consecutive clean runs, not one
- [x] Next.js frontend typecheck passes
- [x] Design-token consistency check passes
- [x] Next.js production build completes
- [x] Firebase authentication and profile creation flow
- [x] Public search, filters, map pins, sorting, counts, cursor pagination, and coordinate fuzzing
- [x] Listing lifecycle, drafts, validation, submission, moderation, photos, amenities, favorites, and editing
- [x] Listing detail, public profiles, messaging, reporting, admin console, homepage, city pages, sitemap, and robots rules

## Priority 1: Launch blockers

**Every item in this section is now checked off (2026-09-05).** That does not mean launch-ready by itself — Priority 2 still holds real product-completeness gaps (listing creation parity, several undefined product concepts, accessibility) and the legal pages still need real operator identity before publication — but every item this project itself defined as a *launch blocker* is done and verified.

### Listing lifecycle and notifications

- [x] Decide the exact listing expiry window: 60 days (warning at day 53)
- [x] Implement the scheduled `PUBLISHED -> EXPIRED` job
- [x] Make expiry idempotent and protected by ShedLock
- [x] Decide whether any patch resets expiry or only deliberate renewal does (the existing `updated_at` clock resets on any PATCH)
- [x] Implement `EXPIRED -> PENDING_REVIEW` renewal
- [x] Warn owners seven days before expiry through the transactional notification outbox
- [x] Track expiry warnings idempotently with `listings.expiry_warned_at` and clear it on renewal
- [x] Enqueue expiry notifications in the transactional notification outbox
- [x] Deliver French notifications for approval, rejection, suspension, reinstatement, warning, expiry, and report acknowledgment through the opt-in SMTP outbox worker
- [x] Replace the `WARN` report action 501 response with transactional owner notification
- [x] Add tests for renewal and notification enqueueing
- [x] Add focused tests for expiry warnings, duplicate job execution, and warning idempotency

### Security and privacy hardening

- [x] Audit every public response for email, phone, Firebase UID, internal status, and exact coordinates
- [x] Audit location fuzzing across search, map, detail, sitemap, metadata, hydration payloads, and images
- [x] Add rate limits for reports, messages, listing creation, uploads, signup, and public search abuse paths
- [x] Review CORS, security headers, TLS, and production cookie/token settings
- [x] Verify ownership and role checks on every mutating endpoint
- [x] Verify soft-delete filtering on every API read path; Firestore mirror remains out of scope
- [x] Decide and implement the PII policy for deleted users
- [x] Create a custom-format database backup and restore it into a clean PostGIS environment

### Production operations

- [x] Add error tracking and production metrics (self-hosted Prometheus scrape, not a hosted APM/error-tracking SaaS — see note below)
- [x] Add metrics for search latency, moderation queue depth, reports, jobs, notifications, and failures
- [x] Document deployment pipeline, migration compatibility, and rollback procedure in `docs/PRODUCTION_OPERATIONS.md`
- [x] Run realistic search load tests and record acceptance thresholds
- [x] Seed production amenities and neighborhood reference data
- [x] Document production backup storage, retention, restore verification, and rollback procedures in `docs/PRODUCTION_OPERATIONS.md`; Firebase and SMTP configuration remain in their dedicated guides

## Priority 2: Product completeness

### Listing creation

- [ ] Match the eight-step prototype in `flows/listing-creation/` — the wizard is now 6 condensed steps (Annonce, Pièces, Chambre, Règles, Photos, Validation), not the prototype's 8
- [x] Replace raw latitude/longitude inputs with a map picker — `LocationPicker.tsx` exists and is wired into the Annonce step
- [x] Implement repeatable rooms and shared/private room semantics (2026-09-08) — all 4 steps done. (1) Structure: `V21`, `ListingRoom`, `ListingRoomRepository`. (2) Read contract: `rooms: ListingRoomResponse[]` on `PublicListingDetailResponse` (`GET /listings/{id}`) and the owner-facing `ListingResponse` — empty array when a listing has no rooms, never null. The four lifecycle actions pass `null`, same as `coverPhotoUrl`/`houseRules`. (3) Write contract: `POST /listings` and `PATCH /listings/{id}` accept a `rooms` array (`ListingRoomRequest`: roomType required, isRentable, isShared, description), full replace, same rule as `amenityCodes`/`houseRules`. (4) Wizard "Pièces" step — a new step 2 of 6 (between Annonce and Chambre), matching the original 8-step prototype's actual fields: a card per room (type select, "C'est la pièce proposée" toggle — mutually exclusive across rooms, matching the prototype's own behavior — "Espace partagé" switch, optional description), quick-add chips per room type, remove button. Verified against a real published listing end to end: saved 3 rooms (one offered, two shared, one with a real description), confirmed in the database, and confirmed they reload correctly into the wizard on reopening. `typecheck`/`tokens:check`/`build` all clean.
- [x] Implement house-rules write and read contracts (2026-09-05/06) — entity, repository, read/write API contract, owner-facing response, detail-page render, and the wizard's Règles step are all done
- [x] Neighborhood reference data exists (`V20`, `/neighborhoods?city=`) and city-membership is now validated on create/update (2026-09-06); neighborhood name itself deliberately stays free text
- [x] Verified full per-step validation and responsive parity at 375px and 1440px (2026-09-10) — walked the whole 6-step wizard live at 375px specifically, since this session's earlier real-photo-upload pass only exercised 1440px. Found it already solidly built, not a gap: step 1 correctly blocks in sequence on missing title, then quartier, then map point, each with a distinct message; step 3 blocks on missing rent; step 4 catches the one-sided quiet-hours window client-side (matching the backend's `validateQuietHours` rule) with its own message; steps 5-6 don't hard-block per-step but the final "Vérification avant publication" review correctly flags missing description and missing photos in red before allowing submission — a legitimate wizard pattern, not a gap. Layout held cleanly at 375px throughout: amenity/room-type chips wrap without overflow, the step-tabs strip is a deliberately contained horizontal-scroll `.scroll-row` (verified via `scrollWidth`/`clientWidth`, not a page-level overflow bug), no element clipped by the fixed Retour/Suivant bar (confirmed with real `getBoundingClientRect()` gaps, not just a screenshot glance). Test draft created during the pass deleted afterward.
- [x] Cap listing photos at 20 per listing; image resizing policy remains open

### Search and discovery

- [ ] Define the product meaning of `recommended` ranking
- [ ] Define the product meaning of featured listings
- [x] Fix amenity chip rapid-click URL state loss (2026-09-05; `SearchResults.tsx` now keeps a synchronous latest-selection ref and syncs it from URL state)
- [x] Fix a fifth fabricated homepage number that survived the "measured, not written" pass (2026-09-09) — `app/page.tsx`'s "Voir les 1 843 annonces" link was a hardcoded literal, sitting one screen below per-city counts that were already wired to `GET /listings/count`. Found by actually loading the homepage rather than reading the component in isolation (dev DB has 29 listings, not 1,843). Fixed with one more real fetch to the same endpoint with no `city` filter, same capped/pluralized rendering the city cards already use. Verified live: now reads "Voir les 29 annonces".
- [x] Fix public profile (`/profile/[id]`) always showing "0" active listings (2026-09-09) — `UserService.publicProfile` had `long activeListings = 0L;` hardcoded behind a comment reading "Wired in phase 09, once listings exist"; listings have existed for a while and the count was simply never connected. Found by checking a real profile (2 real published listings) against the "0" it displayed. Added `ListingRepository.countByOwnerIdAndStatusAndAvailabilityStateAndDeletedAtIsNull` (same PUBLISHED+AVAILABLE search-visibility invariant used elsewhere) and wired it in. Verified live after the API restart: `GET /users/{id}` now returns `"activeListingCount":2` for the same profile that showed 0, and `/profile/[id]` renders "2" in the browser.
- [x] Fix `/favorites` wasting most of the row on desktop (2026-09-09) — the only place in the app using `ListingCard`'s `horizontal` layout was a single always-full-width column, so at 1440px the 116px thumbnail and short price line filled maybe 40% of a 1200px container with empty white space past them. `/listings`' own search results solve the identical "collection of listings" layout with `.results-grid` (1/2/3 responsive columns) and the default vertical card — switched favorites to the same class and dropped `layout="horizontal"` for consistency with every other listing collection in the app, rather than inventing a second layout. Verified at 375/1440px and that add/remove-favorite still round-trips to the DB correctly under the new markup.
- [x] Seed data quality fixed (2026-09-10) — two Rabat listings (`c0e0fdfa…`, `bb61203e…`) were tagged with neighborhood "Gueliz", a real Marrakech district, not a Rabat one; reassigned to real Rabat districts from the `neighborhoods` reference table (`Hay Riad`, `Souissi`). Also found the duplicate-title problem was much larger than first spotted — not two pairs but 6 template titles reused 2–3× each *within the same city* (28 rows total: "Belle chambre dans appartement rénové", "Grande chambre near ISCAE", "Chambre meublée avec balcon", "Studio calme en centre-ville", "Chambre cosy quartier animé", "Chambre lumineuse proche tramway"), which read as duplicate postings in a single city's search results rather than distinct listings. Renamed 16 of them to distinct, real-sounding titles, keeping one of each group as-is; verified `/flatshare/rabat` now shows 4 real neighborhoods (no Gueliz) and 8 listings with no repeated title.
- [x] **Investigating this uncovered a much bigger bug than the item's own wording (2026-09-10).** `?view=map` itself worked fine (verified live). The real defect: `SearchResults.tsx` defaulted `city` to `'Rabat'` whenever the URL had no `city` param at all — not a display quirk, the value it actually sent to `GET /listings`. The backend has always supported a cityless nationwide search (`city` is `@RequestParam(required = false)` in `ListingController`), and the homepage's own main "Voir les 29 annonces" link (`app/page.tsx`) points at bare `/listings` for exactly that reason — but every visitor clicking it silently got Rabat's 8 listings instead, with no indication anything was filtered. There is no city picker anywhere on this page; city only ever arrives via the URL, so this wasn't a "user asked for Rabat" default, it was the page inventing a filter nobody requested. Fixed by making `''` mean "no city filter" throughout — the six spots that read `city`/wrote it back to the URL all forced the `'Rabat'` fallback (`updateUrl`, `setSort`, the main results fetch, the map-pins fetch, `handleLoadMore`) — and adjusted the heading/breadcrumb to say "29 annonces" / "Toutes les villes" instead of silently substituting a city. Verified live: bare `/listings` now returns all 29 real listings spanning all 4 cities (previously exactly 8, all Rabat); `/listings?city=Casablanca` unaffected (7 annonces à Casablanca, unchanged); map view now loads pins nationwide. `tsc --noEmit`/`tokens:check` clean, no console errors, no horizontal overflow at 375px.
- [x] Remove a leftover QA-seed listing polluting real search results (2026-09-11) — while spot-checking `/listings` during an unrelated accessibility pass, "Chambre QA lumineuse, proche du tramway Agdal" turned up as a real, `PUBLISHED` row (owner `dari.qa.owner@example.com` / "Test Proprietaire", a dedicated QA seed account) — indistinguishable from a genuine listing to anyone browsing the site. Confirmed it had no conversations pointing at it (`conversations.listing_id` is `NO ACTION` on delete, so a referenced row would have blocked/orphaned a real thread) before deleting it directly (`listing_photos`/`listing_amenities`/`house_rules`/`favorites`/`listing_rooms` all cascade). Left two other test rows alone — "test" and "chambre test pieces", both `PENDING_REVIEW` under the real account `chaounimouad10@gmail.com`, not public and not obviously safe to delete without asking. Verified: `/listings` dropped from 29 to 28 real annonces.
- [ ] Decide whether to port the mobile filter bottom sheet exactly or keep the disclosure design
- [ ] Add neighborhood landing pages if they are part of the launch SEO scope
- [x] Audited lazy loading across every `<img>` in the app (2026-09-10) — found it already largely done: 5 of 6 usages (`ListingThumb`, `ListingCard`, the publish wizard's photo previews, the messages listing-context thumbnail) already had `loading="lazy" decoding="async"`. The 6th, `ListingGallery.tsx`'s main photo, correctly has neither — it's the listing detail page's above-the-fold LCP element, and lazy-loading the largest above-the-fold image is the specific anti-pattern Lighthouse flags, not an oversight. The one real gap: `account/profile`'s avatar preview had neither attribute; added `decoding="async"` only (not `loading="lazy"` — a 72px avatar near the top of the page is already in the initial viewport, so lazy-loading it wouldn't defer anything real). Real server-side image *optimization* (resizing, format conversion, responsive variants) is a separate, larger backend decision, not a nearby fix — already tracked as "image resizing policy remains open" under Listing creation above; not invented here.

### Messaging and account

- [x] Add unread counts and badges (2026-09-10) — previously "deliberately not built": `ConversationResponse` carried no `unreadCount`, and computing one client-side would have meant fetching every conversation's messages just to render a badge. The real aggregate already existed in spirit — `MessageRepository.findUnreadForUser` backs `markRead` — so added `countUnreadForUser` (same shape, `count()` instead of loading rows) and a new `unreadCount` field on `ConversationResponse`, computed per row in `toConversationResponse` from the *current* user's perspective (messages sent by the other participant, not yet `readAt`). Inbox (`/messages`) now shows a real numeric badge and bolds the name/preview for threads with unread messages; both clear correctly once the thread is opened, since that already calls `PATCH .../read`, which this reuses rather than duplicating. New `MessagingApiTest.conversationListReportsUnreadCount` (backend 128/128, up from 127). Verified live end to end with a seeded 2-message unread thread: badge showed "2", cleared to none after opening the thread, addressee correctly saw "0" unread thereafter — at both 375px and 1440px, no console errors.
- [ ] Add visible read receipts if product-required
- [ ] Decide whether optimistic message sending is required
- [x] Show an explicit unavailable-listing state in old conversation threads — this was already fully built (icon, "Annonce indisponible" label, explanation, catching the `GET /listings/{id}` 404 for anything suspended/expired/removed); the checkbox was just stale. Verified live with a real Chrome session (`chrome-devtools` MCP, signed in as a real account) rather than trusting the code: seeded a test conversation against a listing, suspended it, confirmed the card renders correctly.
- [x] **Two real bugs found and fixed in the same pass (2026-09-09), while verifying the item above in a real browser rather than just reading the code** — the user asked to see the actual rendered thread, which is what surfaced these; neither would have been caught by `tsc`/`build` alone:
  - **The listing-context card scrolled away with the messages instead of staying visible.** First fix attempt (move the card's markup outside the `overflowY: auto` message div) looked right by inspection but measurably did nothing — `getBoundingClientRect()` before/after a script-driven scroll proved the card still moved 1:1 with the page. Root cause: `messages/[id]/page.tsx`'s outer `<main>` used `minHeight: '100vh'`, not a bounded `height`, so it silently grew taller than the viewport and it was the **document** that scrolled, not the inner region the layout was designed around — the header only stayed put because it has its own explicit `position: sticky`, nothing about "outside the scrolling div" made an element fixed on its own. Real fix: `minHeight` → `height: '100vh'` on `main`, which bounds the flex layout the way the code's own comments already described (sticky header, internally-scrolling messages, fixed composer) and made a second, separate bug visible.
  - **The message composer was never actually fixed at the bottom either** — same root cause. On any thread with enough messages to need scrolling, the input/send bar drifted down the page after the last message instead of staying reachable, which is a real usability defect for a chat UI: scroll up to read older messages and the reply box was gone until scrolling all the way back down. Fixed by the same `height: '100vh'` change; verified the composer's `getBoundingClientRect()` stays pinned to the viewport bottom across scroll, on both 1440px and 375px.
  - **Third, smaller fix that fell out of testing the first two**: bounding `main` still left 50px of residual page-level scroll — the site's global legal-links footer (rendered by the root layout, `app/layout.tsx`, on every route) sat right after `main` in the DOM. This is the only route in the whole app that bounds its own layout to the viewport (`grep "height: '100vh'" app/` returns exactly one file; every other page correctly uses `minHeight`, where the footer belongs). Extracted the footer into `components/SiteFooter.tsx`, a client component that hides itself on `/messages/[id]` by pathname and renders normally everywhere else — verified footer still shows on `/`, `/messages` (the inbox list), confirmed absent only on an open thread, and confirmed the residual scroll is now exactly zero.
- [x] Two more real bugs found and fixed on `/account/listings` (2026-09-09), same visual-QA pass: (1) the stat row only tracked Publiées/Brouillons, so an owner whose listings were all mid-review saw "2 au total" sitting above two zeros — added an "En vérification" tile for `PENDING_REVIEW`, matching the exact wording each listing card's own status badge already uses, and widened the grid to `repeat(auto-fit, minmax(140px, 1fr))` so it still wraps cleanly at 375px with four tiles instead of three. (2) The status-badge row was missing `flexWrap: 'wrap'` — present on its two sibling rows just below it, absent here — so at narrow widths a long title plus the (non-shrinking, `whiteSpace: 'nowrap'`) badge overflowed the card; confirmed with `getBoundingClientRect()` before (badge right edge 8.8px past the card) and after (contained) the fix, at both 375px and 1440px.
  
  All three verified with real DOM measurements (`getBoundingClientRect`, `scrollHeight`/`clientHeight` diffs) in an actually-authenticated Chrome session, not just by reading the code or trusting a screenshot — a short test thread doesn't scroll far enough to reveal the composer bug at all, which is exactly why it went unnoticed until a 30-message thread was seeded specifically to test it.
- [x] Define and implement profile-completion rules and prompt (2026-09-10) — `/account`'s "Profil complet" tile showed a hardcoded "85%" tied to nothing, next to a "Compléter le profil" button and a header "Modifier" button that both had no `onClick`. Defined a defensible default rule set (five equally-weighted checks already available on `Me`: avatar, bio, city, phone, verified email) rather than leaving the number fake — same "stop inventing numbers" discipline as the payments/security fixes. Computed live and verified against the real account: 1 of 5 met (city only) renders "20%", not the old static "85%". The tile now hides itself entirely at 100% rather than telling a finished profile to keep completing itself. Both dead buttons now link to `/account/profile`. Also removed a third dead button on the same page, "Tout voir" — it had no onClick and nothing to reveal, since the settings list below it is 4 items that already render unsliced.
- [x] **This checkbox was stale — corrected 2026-09-09.** It claimed fabricated account data was already excluded; `/account/payments` and `/account/security` both still had it. `/account/payments` rendered a specific fake card ("•••• 4582 • Expire fin 2028"), a fake IBAN, and "3 reçues" directly under a header honestly reading "Pas encore disponible" — replaced with an honest empty state and disabled the dead "Ajouter un moyen" button (no `onClick`, ever). `/account/security` was worse: a hardcoded "2 appareils actifs sur votre compte" and "Dernière modification il y a 3 mois" sat one section above a disclaimer stating "Cette page ne simule pas de données d'appareils" — directly contradicting it — and its "Vérification de compte: Validé" hardcode disagreed outright with `/account/profile`'s own real "Non vérifié" badge for the same account. Rewired security as a client component reading real `/users/me` data (`verification`, `emailVerified`) for the two fields that exist; the two that don't (password-change timestamp, device sessions) now say "Non disponible" instead of inventing numbers, and the disclaimer is no longer contradicted by its own page. "Modifier" disabled for the same reason as payments' "Ajouter un moyen".
- [x] `/account/notifications` converted from a fully static mock to the honest "coming soon" pattern (2026-09-10) — was a hardcoded `TOGGLES` array with no `onClick` on any switch, a "Paramètres avancés" button going nowhere, and a "Canal préféré" row whose chevron implied a channel picker that doesn't exist. Real per-channel preferences would need a migration + endpoint + frontend feature (there is still no backend concept of notification preferences, and no SMS sender exists — only `SmtpNotificationSender`), so this went the same direction as payments/security rather than building that unprompted: rewired as a client component that states plainly what's actually true (the SMTP outbox worker really does email the account's real address — now fetched live from `/users/me` and shown, not a placeholder — for messages, listing status changes, and moderation outcomes) and marks SMS/per-channel control "Pas encore disponible sur Dari." instead of faking it. Verified live at 375/1440px, no console errors, `tsc`/`tokens:check` clean.
- [ ] Define verification-tier semantics and phone verification scope
- [x] Add password recovery to `/sign-in` (2026-09-09) — there was no "forgot password" link anywhere in the app, not even a dead one; a locked-out user had no self-service path at all. Cheap to add because Firebase's client SDK owns the whole flow (`sendPasswordResetEmail` sends the email itself, no backend endpoint needed) — added `sendPasswordReset()` to `lib/firebase.ts` and a link under the password field. Deliberately reports success even when Firebase says `auth/user-not-found`, to avoid an account-enumeration leak the project's own email-enumeration-protection setting shouldn't be relied on to prevent; a bad email format or a network failure still surfaces as a real error. Verified live end to end against the real account (a genuine reset email was sent) and the empty-email guard. One nested-`<label>` accessibility issue caught before shipping: the button's text was being folded into the password input's accessible name by the browser's label-association algorithm — moved it outside the `<label>`.
- [x] Surface a report's free-text "details" on the admin moderation queue (2026-09-09) — `ReportResponse` has always included `details`, but the grouped `/admin/reports` queue (`AdminReportQueueItem`) never carried it, so a moderator saw only the coarse reason category, never the context a reporter actually wrote — on an anti-scam platform where that free text is often the one thing that says what actually happened. Added `details: List<String>` to `AdminReportQueueItem`, aggregated the same way `reasons` already is (one entry per non-blank report detail, in report order), and rendered it in the admin queue card as quoted text under the reason chips. Backend tests (`AdminApiTest`/`ReportApiTest`, 25/25) and the full suite (127/127) pass; verified live end to end — submitted a real test report with details text through the real report dialog, confirmed it rendered correctly in the queue, dismissed it, and confirmed via direct DB query that `reports.details` had held the text all along; only the queue's own projection was missing it.
- [x] **Fix every "preview a non-public listing" link 404ing for its own owner and for admins (2026-09-09)** — `getPublicOrOwnerListingDetail` has always had a deliberate `owner || admin` bypass so a draft, pending, rejected or suspended listing stays visible to the one person previewing it or the moderator reviewing it. But `/listings/[id]/page.tsx` is a Server Component whose `GET /listings/{id}` fetch never carries a token — there is no way for a Server Component to reach the browser's Firebase session — so that bypass could never fire through it. Every "Voir" link on `/account/listings` and every "Voir l'annonce" link in the moderation queue point straight at this page, so both silently 404'd on exactly the listings they exist to preview: anything that isn't PUBLISHED + AVAILABLE, which for a moderator reviewing a PENDING_REVIEW listing is *every single case*. Found live, not by reading the code — clicking "Voir l'annonce" from the moderation queue during an admin-action test landed on "Annonce introuvable" for a listing that very much exists. Fixed by splitting the page: the fast unauthenticated SSR fetch stays exactly as it was (correct and desired for the common case — a real anonymous visitor, and good for crawlability), extracted into a shared `ListingDetailContent` component; when that fetch 404s, a new client component (`AuthenticatedPreview`) retries with the real Firebase token before accepting "not found" as final. `generateMetadata` deliberately still always reports not-found/noindex regardless — a crawler never carries a token either, so the safe metadata is correct independent of who's actually looking. Verified live: an admin now sees the real listing (photos, description, amenities, rules) for a suspended listing and for a pending-review listing via the exact "Voir l'annonce" link that was broken; a bogus/nonexistent id still correctly 404s. `tsc --noEmit` clean. **Caution for next session: do not run `next build` while the dev server is live** — it overwrites the same `.next` directory the dev server depends on and breaks it; this was hit twice this session and needed a dev-server restart both times. Owner-side verification (as opposed to admin) was not done live — no separate real owner account was available this session — but the fix is the identical code path gated by the same `owner || admin` boolean, so this is inferred rather than independently observed.

### Test suite

- [x] **Fixed 2026-09-08 — the coordinate half of the flakiness.** `ListingApiTest.publicDetailIsVisibleForPublishedListing` and `FavoriteApiTest.addListAndRemoveFavorite` asserted the response did *not contain* a substring of the unfuzzed coordinate (e.g. `"longitude":-6.8498`). But `LocationFuzzer` derives its offset from the listing's UUID, random per run, so whenever the offset landed small the fuzzed value simply *started with* the original's digits (`-6.8498` → `-6.8498304629046105`) and the substring check tripped. Never a data-isolation problem. Both now compare numerically against `LocationFuzzer.fuzz(...)`'s own output, which is deterministic given the id, so offset size cannot affect the result. Two things fell out of the fix: a new `LocationFuzzerTest` covering the fuzzer's own guarantees (determinism, that the point always moves, and that 1,000 sampled offsets all stay inside the 200m radius) — it had **no test at all** before, which is why these API tests were left guessing at digit prefixes; and `AbstractIntegrationTest.exactNumbers()`, because RestAssured parses JSON numbers as **float** by default, which holds ~7 significant digits and silently shifted the eighth by ~1e-6. Verified with 5 consecutive clean runs, not one.
- [x] **Fixed 2026-09-08 — and it was never actually the shared-container isolation problem this was filed under.** `ListingSearchOptimizationTest.distanceSortWorks` has its own `@BeforeEach` that deletes every `PUBLISHED`/`AVAILABLE` listing before each test and JUnit runs this suite sequentially (no parallel execution configured), so cross-class data leakage was never possible for this test — that theory didn't survive checking. The real cause: `listings.location` is a `geography(Point,4326)` column, so the query's `ORDER BY ST_Distance(...)` sorts using PostGIS's geodesic (WGS84 spheroid) distance, while the test re-verified the order using its own `haversineMiles` helper — a spherical-Earth approximation. The two models disagree by a few metres for two listings that land nearly equidistant from the search origin (10 listings scattered randomly within ~15km each run), which was occasionally enough to flip their apparent relative order and fail a strict `>=` assertion — a real disagreement between two distance formulas, not a bug in the query, the pagination, or test isolation. Fixed by asserting against `ST_Distance` itself (queried directly via `JdbcTemplate`, the exact computation the query ordered by) instead of a Java reimplementation of it — removes the discrepancy at the root rather than tolerating it with an epsilon. Verified with 8 consecutive clean runs across different random seedings, not one.

### Design system and content

- [ ] Reconcile design-system source components with the compiled bundle version — **investigated 2026-09-10, left unchecked, flagging rather than guessing.** `design-system/` is a self-contained package (its own `readme.v2-notes.md`, `_ds_bundle.js`/`_ds_manifest.json`, plus an older `_legacy-v1-build/` of the same two files) that the actual Dari app does **not** import from at all — `apps/web/src/components/ds/*.tsx` are hand-ported TSX, each with its own documented list of deliberate departures from the `.jsx` sources (see e.g. `ListingCard.tsx`'s header comment). So this item isn't about anything the running app does; it's about whether the design-system package's own compiled bundle still matches its own `.jsx` sources, using build tooling that isn't documented anywhere in the package. Reconciling it blind risked guessing at a pipeline I can't see the shape of — needs whoever owns that tooling (or the item itself) to say what's actually drifted before it's actionable.
- [ ] Recover or replace final logo and photography assets
- [x] Resolve the five WCAG AA token contrast failures (2026-09-08) — no committed audit existed naming which five, so computed real WCAG 2.1 contrast ratios for every plausible token pair actually used in the app (`colors.css` + a grep of real component usage) and found exactly five failing, matching the count: `--warning` unusable as text (2.71–2.95:1, need 4.5 — every call site that needed it had already learned to hand-code `saffron-700` instead; see `Badge.tsx`/`LocationPicker.tsx`'s prior workaround), `--border-default` on a white card (1.57:1, need 3 — the resting boundary of `Checkbox`/`Radio`/the secondary `Button`), `--border-hairline` used as `Input`/`Select`/`Textarea`'s resting field border (1.30:1 — a decorative-only token pressed into a WCAG-relevant role it was never meant for), and `--border-focus` against the page background (2.91:1, need 3). Fixed at the token level, not by patching individual components: `--warning` repointed to `saffron-700` (the value everyone was already reaching for by hand — the two workarounds are now removed), `--border-default` repointed to `sable-500`, `--border-focus` repointed to `--brand`/`clay-500` (unifying it with the already-brand-coloured `--focus-ring`). `--border-hairline` itself was deliberately left alone — it's correctly soft for its real job (`Card` outlines, list dividers, which WCAG 1.4.11 doesn't cover), so `Input`/`Select`/`Textarea` were redirected to `--border-default` instead of darkening the shared decorative token for everyone. `IconButton`/`Tag`/`Tabs`/`ListingCard`/`ReportDialog` also reference `--border-hairline` and were **not** audited for whether their own usage is decorative or control-identifying — flagged here rather than silently left, since that's a real open question the existing "Complete final responsive and keyboard accessibility review" item should cover.
- [x] **Resolved the `--border-hairline` open question above (2026-09-11).** Checked each flagged component against WCAG 1.4.11 (does the border identify a control's boundary, or is it a decorative divider?): `Card`, `Badge`, `ListingCard`'s outer card border, and `Tabs`' tablist bottom rule are all non-interactive or have another way to read as a control (the active tab's brand-coloured underline) — correctly left on `--border-hairline`. `IconButton`'s `secondary` variant and `Tag`'s unselected state, though, are each a real control whose *only* resting-state boundary is that border (no shadow or background shift distinguishes `Tag` from its card) — moved both to `--border-default`, the same token `Input`/`Select`/`Textarea` were redirected to in the original audit. Also found two more instances of the exact same bug the original audit had already fixed for the shared `Input`/`Textarea` components but couldn't catch because they don't use them: `ReportDialog.tsx`'s hand-rolled reason radio row and its `<textarea>` were still on `--border-hairline`; moved both to `--border-default` too. Separately, `Tabs.tsx` carried a stale comment claiming `--text-subtle` "fails AA at this size" — that token was already repointed to `sable-600` earlier in this same audit; removed the now-incorrect comment. `tokens:check` and `tsc --noEmit` clean; verified live that the amenity filter chips render with a clearly visible border against the filter panel. `tokens:check`, `tsc --noEmit`, and `next build` all clean; this is a visible design change (form-field borders are now noticeably darker) and needs a real look in the browser, not just the automated checks, before being treated as fully done.
- [x] **Fix `Dialog`'s focus-management effect stealing focus on every keystroke (2026-09-09)** — its `useEffect` depended on `[open, onClose]`, but every call site passes `onClose` as an inline arrow function (`onClose={() => setX(null)}`), a fresh reference on every parent render. Typing into any field inside any dialog re-rendered the parent, which re-ran the effect, which called `panelRef.current.focus()` and yanked focus off the input back onto the dialog panel — so only the first character of anything typed (not pasted) into any dialog anywhere in the app ever registered. Found live, not by reading the code: while testing admin moderation actions end to end for the first time this session (rather than just viewing the pages), typing a rejection reason and a report's details both silently kept only their first character, confirmed with a direct DOM `.value` read, not just the accessibility snapshot's stale text. Fixed with the standard "latest ref" pattern — a `useRef` holding the current `onClose` for the Escape-key handler, with the focus-management effect itself depending only on `[open]`. Verified live end to end after the fix in both affected dialogs: full typed strings now land in the DOM, submit correctly, and persist correctly server-side (`listings.rejection_reason` and `reports.details` both checked directly against Postgres).
- [x] Fix six places mixing the `font` shorthand with a separate `lineHeight` on the same style object (2026-09-09) — a real React dev-mode console warning ("Removing a style property during rerender when a conflicting property is set can lead to styling bugs"), not just noise, surfaced live while testing the report-submission flow. `--type-body`/`--type-body-sm` already carry their own line-height as part of the shorthand; six sites (`ReportDialog.tsx` ×2, `account/profile`, `account/security`, `admin/listings`, `messages/[id]`, `profile/[id]`) layered an explicit `lineHeight` on top of it regardless. Expanded each to the shorthand's own longhand parts (`fontWeight`/`fontSize`/`fontFamily`) rather than dropping the intentional looser line-height. No visual change; confirmed the console warning is gone by reopening the report dialog after the fix.
- [ ] Complete final responsive and keyboard accessibility review
  - [x] **Fix `Dialog` not trapping keyboard focus (2026-09-11)** — found by actually tabbing through an open dialog rather than reading the component: `Dialog.tsx` moved focus in on open and back to the opener on close, and closed on Escape, but nothing stopped `Tab`/`Shift+Tab` from walking off the end of the panel's focusable elements onto whatever the dialog happened to sit next to in the DOM. Reproduced live on `ReportDialog`: opened it from a listing page, pressed Tab six times, and focus landed on the page's own "Données de localisation" footer link — content that was still visually hidden behind the modal scrim and, per `aria-modal="true"`, supposed to be unreachable while the dialog was open. Since every dialog in the app (`ReportDialog`, every admin moderation action dialog, the contact flow) goes through this one shared component, fixed it once at the source: the existing `keydown` listener now also intercepts `Tab` at the panel's first/last focusable element and wraps it back around, rather than letting it fall through to the document. Verified live in both directions — forward `Tab` from the last element (`Annuler`) wraps to the first, `Shift+Tab` from the first wraps back to `Annuler` — and confirmed `Escape` and the close-returns-focus-to-opener behaviour are unaffected. `tsc --noEmit` clean.
  - [x] **Spot-checked three candidates from this item's own list (2026-09-11) — all already correct, no code changed.** Brought up the full local stack (Docker Postgres/MinIO, the Spring Boot API, seeded listings) to check live rather than by reading the source, since a static read can't show real Tab order or a rendered focus ring:
    - `ListingCard`'s save button vs. its title link, in the actual `/listings` grid: DOM order puts the heart `IconButton` before the card's anchor, so `Tab` visits each card's save button then its title, in top-to-bottom reading order, and the second `Tab` correctly moves to the *next* card rather than skipping or looping. Confirmed with real `Tab` presses plus screenshots — both the button and the whole card show a clearly visible terracotta ring when focused.
    - `Select`/`Switch`'s focus indicator (no live-rendered `Checkbox` or `Radio` exist in the app yet — the components exist in `ds/` but nothing currently mounts them, so only `Select` and `Switch` could be checked live): both were suspected of "relying on default outline," but that's actually the intended design — `app.css`'s global `*:focus-visible` rule paints every plain element a themed 2px `var(--brand)` outline, and only the three controls that hide their real input behind a custom-painted box (`Checkbox`, `Radio`, `Switch`) additionally need the `--focus-ring` box-shadow token, because the outline would land on the invisible input instead of the visible one. Tabbed to a real `Switch` and a real `Select` in the publish wizard and confirmed both: `outline: 2px solid rgb(181, 85, 53)`, clearly visible against the page.
    - Admin moderation's "Rejeter" dialog, now that `Dialog` traps focus: created a throwaway admin account (`qa-a11y-test@dari.local`, promoted to `ADMIN` directly in the dev DB, deleted afterward) to reach `/admin/listings` and opened a real rejection dialog. Typed a full rejection reason and confirmed it landed completely in the textarea (the first-character-only regression from the earlier `onClose` fix has not come back here), then confirmed `Tab` from `Rejeter l'annonce` (last) wraps to `Fermer` (first), `Shift+Tab` from `Fermer` wraps back to `Rejeter l'annonce`, and `Escape` closes the dialog and returns focus to the row's `Rejeter` button.
  - [x] **Fix the publish wizard's step-chip jump dropping keyboard focus to `<body>` (2026-09-11)** — found live, not by reading the source: once a step is completed, `publish/page.tsx`'s stepper renders it as a real `<button>` back to itself (`onClick={() => setStepIndex(index)}`), which is a genuine, independently reachable keyboard path distinct from `Suivant`/`Retour`. But the instant that click fires, the step it points at becomes *current* rather than *done*, so it re-renders as a plain `<span>` instead of a button — the exact element that had focus unmounts mid-click, and the browser drops focus to `<body>`, stranding a keyboard user with no sense of where the page put them. `Suivant`/`Retour` never had this problem: that button stays mounted at the same position across every step, so focus already survives there without help. Fixed by giving the step-heading row (`Étape X sur 6` / step name, already always-rendered) a ref and `tabIndex={-1}`, and moving focus there via a `useEffect` keyed off `stepIndex`, armed only by the step-chip button's own `onClick` (a ref flag) so `Suivant`/`Retour`'s already-working focus retention is untouched. Verified live end to end with a throwaway account (`qa-a11y-session2@dari.local`, deleted afterward): filled step 1, advanced to step 2, jumped back to the "Annonce" chip with a real `Enter` press, and confirmed focus lands on the step heading with a clearly visible ring (screenshotted) instead of `<body>` — with the title and quartier field values still intact, and a further real `Tab` continuing naturally into the title field. `tsc --noEmit` clean.
  - [x] **Fix the same unmount-on-click focus loss on "Retirer la pièce" in the publish wizard (2026-09-11)** — found by generalizing the step-chip fix above rather than assuming it was the only instance: the "Pièces" step's rooms are each a `<Card key={room.key}>` with their own remove `IconButton`, keyed by a stable per-room key (not index), so removing a room truly unmounts its whole card — including whichever button had focus — rather than a later room sliding into the same DOM slot. Confirmed live with a throwaway account (`qa-a11y-rooms@dari.local`, deleted afterward): added two rooms, focused "Retirer la pièce 1", pressed a real `Enter`, and watched focus drop to `<body>`. Fixed the same way as the step chips: gave the step's always-rendered intro paragraph ("Ajoutez chaque pièce…") a ref and `tabIndex={-1}`, and the remove button's `onClick` now arms a ref flag that a `useEffect` keyed on `rooms.length` consumes to focus it. Re-verified live after the fix — focus lands on the intro paragraph with a clearly visible ring (screenshotted), and the remaining room ("Salon") is untouched. `tsc --noEmit` clean.
  - [x] **Fix the same unmount-on-click focus loss a third time, on "Supprimer la photo" in the publish wizard (2026-09-11)** — checked deliberately rather than assumed, since two hits on the same file's pattern is exactly the point where "probably fine elsewhere" stops being safe to trust: `grep`ped every `icon="trash` in `publish/page.tsx` and found the Photos step's remove button has the identical shape — `<li key={photo.id}>` per photo, its own `IconButton` calling `void removePhoto(photo.id)`. Reproduced live with a throwaway account (`qa-a11y-photos@dari.local`, deleted afterward, along with its draft listing and uploaded photo files): pushed a real draft through all four prior steps, uploaded two photos via the actual file input, focused "Supprimer la photo 1", pressed a real `Enter`, and watched focus drop to `<body>` exactly like the other two. Fixed the same way: gave the step's always-rendered "Ajouter des photos" heading a ref and `tabIndex={-1}`, armed via the remove button's `onClick` and a `useEffect` keyed on `photos.length`. Re-verified live after the fix — focus lands on the heading with a clearly visible ring (screenshotted), photo 1 is gone, and the surviving photo correctly promotes to "Couverture". `tsc --noEmit` clean. Did not go looking further than this file for the same pattern elsewhere in the app — nothing else currently renders a keyed list of removable items with a button of its own, per the earlier `icon="trash` grep across `app/` and `components/`.
  - [x] **Fix the messages compose box dropping keyboard focus to `<body>` on every message sent (2026-09-12)** — a different mechanism from the publish wizard's three unmount-on-click bugs, but the same symptom: `messages/[id]/page.tsx`'s composer `<input>` has `disabled={sending}` and the "Envoyer" button has `loading={sending}` (which `Button.tsx` also renders as `disabled`), so the instant a send starts, whichever control had focus (the input via a real `Enter` keypress, or the button via a click) gets disabled — and a browser blurs any element it disables, dropping focus to `<body>`. Nothing re-focused the input once `sending` flipped back to `false`, so a keyboard user sending several messages in a row had to click back into the field before every single one. Reproduced live: created two throwaway accounts (`qa-a11y-messages-a@dari.local`, `qa-a11y-messages-b@dari.local`), published a throwaway listing as A to get a real conversation between them, opened the thread as B, typed a message, pressed a real `Enter`, and confirmed via `document.activeElement` that focus landed on `<body>` after the send completed. Fixed with the same "ref flag armed at submit, consumed by a `useEffect` keyed off the async state going back to idle" pattern already used for the wizard: a `composerRef` on the `<input>`, a `shouldRefocusRef` armed in `onSubmit` only when a send actually starts, and a `useEffect` on `[sending]` that refocuses the input once `sending` goes back to `false` — covering both the success and failure paths, since both go through the same `finally`. Re-verified live after the fix: sent a second message with a real `Enter` press and confirmed via both `document.activeElement` and the accessibility snapshot (`focusable focused`) that the input keeps focus. Test accounts, listing, photo file, and conversation all deleted from the dev DB/`uploads/` afterward. `tsc --noEmit` clean.
  - [x] **Fix the same disable-while-submitting focus loss on the sign-in "Se connecter" button (2026-09-12)** — same root mechanism as the messages composer, found by generalizing it to every other `disabled={<async-state>}` control in the app (grepped `disabled={` against known loading/submitting/sending/saving state names). `sign-in/page.tsx`'s submit button carries `disabled={submitting}`; a keyboard user who tabs from the password field past the eye-toggle and "Mot de passe oublié ?" to "Se connecter" and presses `Enter` loses focus to `<body>` the instant the request starts, and nothing gave it back — so on a failed login the `role="alert"` error still announces correctly, but a keyboard-only user has no idea where the page put them afterward and has to tab in from the top of the document to retry. Reproduced live with a real keyboard sequence (fill email/password, three real `Tab` presses to reach the button, confirmed via `document.activeElement`, then a real `Enter`) against a deliberately wrong password: focus landed on `<body>` after the error rendered. Fixed with the same ref-flag + `useEffect` pattern: a `submitRef` on the button, armed by `shouldRefocusSubmitRef` at the start of `handleSubmit`, consumed by a `useEffect` on `[submitting]` that refocuses the button once the request settles (success or failure, via the shared `finally`). Re-verified live after the fix — same keyboard sequence, confirmed via the accessibility snapshot (`focusable focused`) that "Se connecter" keeps focus once the error appears. `tsc --noEmit` clean.
  - [x] **Fix the identical bug on the sign-up "Créer mon compte" button (2026-09-12)** — `sign-up/page.tsx`'s submit button has the exact same `disabled={submitting}` shape as sign-in's, flagged there as a plausible next instance. Fixed by code inspection (same `submitRef` + `shouldRefocusSubmitRef` + `useEffect` on `[submitting]` pattern as sign-in, not re-litigated here) rather than reproducing the pre-fix bug separately, then verified the fix live: created a throwaway account (`qa-a11y-signup-dup@dari.local`), reloaded the sign-up form, filled it again with the same address to force a real "email already in use" failure, tabbed to "Créer mon compte", pressed a real `Enter`, and confirmed via the accessibility snapshot `button "Créer mon compte" focusable focused` right where the "Cette adresse e-mail est déjà utilisée." alert renders. Throwaway account deleted from the dev DB afterward. `tsc --noEmit` clean.
  - [x] **Fix the identical bug on account/profile's "Enregistrer" save button (2026-09-12)** — a third instance of the same `disabled={<async state>}` shape (`disabled={saving}`), this one on a form saved repeatedly across a session rather than a one-shot auth form, so the loss recurs every time someone edits their profile. Fixed the same way: a `saveButtonRef`, a `shouldRefocusSaveRef` armed at the start of `handleSubmit`, a `useEffect` on `[saving]`. Verified live with a throwaway account (`qa-a11y-profile-save@dari.local`): tried to force the server-side validation error path first (a 1-character display name, a 650-character bio against the API's `@Size` limits), but both were caught by the browser's own HTML `minLength`/native validation or turned out to fit the server's actual limit, so neither reached the async handler. Fell back to verifying the same `!saving` transition on a real successful save instead — clicked "Enregistrer", watched it disable mid-request, and confirmed via the accessibility snapshot that it read `button "Enregistrer" focusable focused` again once "Profil enregistré." appeared, which is the same `finally`-driven transition the failure path would go through (the effect fires on `!saving` regardless of outcome, exactly as already proven for the messages composer's success/failure symmetry). Throwaway account deleted from the dev DB afterward. `tsc --noEmit` clean.
  - [x] **Fix the same bug on both "Contacter" buttons — `profile/[id]/ContactButton.tsx` and `listings/[id]/ContactOwnerButton.tsx` (2026-09-12)** — a fourth and fifth instance, found by finishing the same grep sweep: `ContactButton` has `disabled={pending}`, `ContactOwnerButton` has `disabled={contacting}`, both losing focus to `<body>` on a failed contact attempt (a success navigates away, so only the failure path is actually affected). Fixed both with the same ref-flag + `useEffect` pattern. Verified live on `ContactButton` against a genuine server-side failure — the API rejects starting a conversation with yourself — using a real account: signing up a fresh one hit this session's own local `SIGNUP` rate limit (an hour-long IP-scoped bucket, exhausted by the QA accounts created earlier in this same session), so rather than wait it out, reused the Firebase account already created by that failed sign-up attempt (its Firebase-side account creation had already succeeded before the rate-limited `POST /users` step), reading its uid out of the browser's own IndexedDB-backed Firebase session and inserting the matching `users` row directly to give it a working backend account. Visited that account's own public profile and clicked "Contacter": confirmed via the accessibility snapshot `button "Contacter" focusable focused` right where "Vous ne pouvez pas démarrer une conversation avec vous-même" renders. `ContactOwnerButton` was not independently reproduced or re-verified — fixed by the same code-level reasoning as `ContactButton` (identical shape, identical mechanism), consistent with how sign-up's fix was applied off of sign-in's live proof. Throwaway account and its dangling Firebase-side registration are the one loose end here — the `users` row was deleted from the dev DB, but the Firebase Auth account itself has no cleanup path from this session's tooling. `tsc --noEmit` clean.
  - [x] **Fix the same bug on the listing detail page's favourite ("Enregistrer"/"Retirer des favoris") button, `listings/[id]/ListingGallery.tsx` (2026-09-12)** — a sixth instance, `disabled={savePending}`, plausibly the single highest-traffic one app-wide since it's the heart button on every listing detail page (the grid card's own heart button was checked separately in an earlier session and doesn't share this bug — it doesn't self-disable during the request). Fixed with the same ref-flag pattern. Verified live: created a throwaway account (`qa-a11y-gallery-save@dari.local`) using the same Firebase-account-first/Postgres-row-second technique as the contact-button fix above (this session's `SIGNUP` rate limit is still exhausted), opened a real seeded listing, clicked the heart button, and confirmed via `document.activeElement` and the accessibility snapshot (`button "Retirer des favoris" focusable focused`) that it kept focus through the toggle. Throwaway account and favorite row deleted from the dev DB afterward. `tsc --noEmit` clean.
  - [x] **Fix the same bug on account/profile's other two async buttons — the avatar photo button and "Supprimer définitivement mon compte" (2026-09-12)** — the seventh and eighth instances, `disabled={avatarBusy}` and `disabled={deleting}`, alongside the page's own already-fixed `disabled={saving}`. The avatar button is a subtler case: its own `onClick` only opens the native file picker, synchronously; `avatarBusy` flips true later, inside the hidden `<input type="file">`'s `onChange` once a file is actually chosen — but a browser returns focus to whatever opened a file picker once it closes, so by the time the upload starts, this same visible button is what's live and gets disabled/blurred. Fixed both with the same ref-flag pattern. Verified live with a throwaway account (`qa-a11y-profile-buttons@dari.local`, same Firebase-account-first/Postgres-row-second technique — the `SIGNUP` rate limit is still exhausted): drove a real avatar upload through the file input (a generated 300×300 PNG) and confirmed via `document.activeElement` and the accessibility snapshot (`button "Changer la photo" focusable focused`) that it kept focus through the upload. For "Supprimer…", a genuine server failure was worth engineering rather than skipping, since the button's success path calls `window.location.href` before `setDeleting(false)` ever runs — only the failure branch can ever trigger this fix, and that branch is what a real bug report would hit. Patched `window.confirm`/`window.prompt` to auto-accept the two confirmation dialogs and monkey-patched `window.fetch` in the page (via `evaluate_script`, not the app's code) to reject only the `DELETE /users/me` call, then clicked the real button: confirmed via the accessibility snapshot `button "Supprimer définitivement mon compte" focusable focused` right where "Suppression impossible." renders — proving the fix works on the one path that actually needs it, without touching any real account. Throwaway account deleted from the dev DB afterward. `tsc --noEmit` clean.
  - [x] **Fix `favorites/page.tsx`'s "Voir plus" pagination button, plus a genuinely new unmount-on-click bug found alongside it on the same page's remove-from-favorites button (2026-09-12)** — the ninth instance of the `disabled={loadingMore}` pattern, but this one had its own wrinkle worth catching: the button is conditionally rendered on `nextCursor` (`{nextCursor ? <button>… : null}`), so loading the *last* page unmounts the very button the refocus effect was about to target, in the same update that clears `loadingMore`. First live check reproduced exactly that: with a throwaway account (`qa-a11y-favorites@dari.local`, same Firebase-account-first/Postgres-row-second technique) and 21 directly-inserted test favorites (real UI clicks would have taken 21 round trips for what is just test-data setup, not the behavior under test), clicking "Voir plus" loaded the remaining item, made it the last page, and focus landed on `<body>` even with the ref-flag fix in place, because `loadMoreRef.current` was already null by the time the effect ran. Fixed by falling back to a second, always-rendered target when the primary one is gone: `(loadMoreRef.current ?? headingRef.current)?.focus()`, reusing the page's own `<h1>` (given a ref and `tabIndex={-1}`) as that fallback. Re-verified live — accessibility snapshot confirmed `heading "Mes annonces sauvegardées" focusable focused` after the same click. While reading this file to fix that, found a second, unrelated bug in the same component: `handleRemove` filters the removed listing straight out of `items` in the same tick as the click (`setItems((current) => (current ?? []).filter(...))`), which unmounts the removed card's `ListingCard` -- including its own heart/"Retirer des favoris" `IconButton`, exactly the button that had focus -- and drops focus to `<body>`. This is the same unmount-on-click pattern already fixed three times in the publish wizard, just newly found here; the `onSave={removingId === listing.id ? undefined : …}` guard inside `ListingCard`'s render is dead code for this bug in practice, since the card is already gone from `items` by the time it would matter. Fixed the same way as the wizard: armed a flag at the point of removal, consumed by a `useEffect` keyed on `items`, refocusing the same `<h1>` fallback target. Verified live with a real click on a real "Retirer des favoris" button: confirmed the card disappeared (count dropped 21→20) and focus landed on the heading (`focusable focused` in the snapshot), not `<body>`. Throwaway account and its 21 test favorites deleted from the dev DB afterward. `tsc --noEmit` clean.
  - [x] **Fix `publish/page.tsx`'s four photo-step buttons (upload, move, set cover, delete) sharing `disabled={photoBusy}` (2026-09-12)** — the tenth instance, and the first with real texture: unlike every prior fix, most of these buttons stay mounted through the async call, so simply jumping to the shared heading (the wizard's usual fallback) would have been a worse fix than returning focus to the exact button that was clicked. Added `armPhotoBusyRefocus()`, called right before each of the four functions' `setPhotoBusy(true)`, which captures `document.activeElement`; a `useEffect` on `[photoBusy]` restores it once the request settles. Found two distinct reasons that capture can come back unusable, both live-reproduced rather than guessed, both needing the existing `photosHeadingRef` as a fallback: (1) the delete button's `<li>` unmounts entirely (the pre-existing unmount fix from an earlier session), and (2) — found only by testing, not by reading the code — a move button can land back on a button that is *disabled by its own new position*: moving photo 1 right swaps it into the last slot, where "move right" is always disabled for whatever sits last, so the very button just clicked becomes structurally unfocusable the instant the swap lands, and `.focus()` on a disabled control is a silent no-op, not an error. A third case surfaced the same way while testing "make cover": once a photo becomes the cover, its own "Définir comme couverture" button unmounts (`{!photo.isCover && …}`), so that action falls back to the heading too. The fix checks both `el.isConnected` and `!(el instanceof HTMLButtonElement && el.disabled)` before trusting the captured element. Verified live end to end with a throwaway account (`qa-a11y-publish-photos@dari.local`) and two real uploaded photos: confirmed the upload button keeps focus on itself after a real file upload (`focusable focused`), confirmed the edge-position move lands on the heading instead of `<body>`, confirmed "make cover" also lands on the heading once its button unmounts, and confirmed the already-fixed delete case is unaffected by the new effect coexisting with it. Draft listing, its photos (DB row and uploaded files), and the account all deleted afterward. `tsc --noEmit` clean.
  - [x] **Fix `account/listings/page.tsx`'s four card action buttons plus its own "Voir plus" (2026-09-12)** — the eleventh instance, and the richest one yet: `runAction` (backing Soumettre/Chambre trouvée/Remettre disponible/Supprimer) shares `isPending` the same way, but its four call sites diverge in what happens to the clicked button on success. Delete removes the whole `<article>` (full unmount, same as the publish wizard's photo delete). The other three patch the listing in place and can hide their *own* button once the status changes — submitting a draft flips `canSubmit` false, so "Soumettre" itself vanishes the instant it succeeds — the same "gone/disabled by its own outcome" shape as the publish wizard's move/cover buttons, just via a conditional render instead of a disabled edge. Added a per-card fallback rather than only the page heading, since a keyboard user managing several listings loses their place if every action bounces them to the top: each card's `<h3>` title got a ref (a `Map<string, HTMLHeadingElement>` keyed by listing id, via a callback ref) and `tabIndex={-1}`, used when the captured button is gone or disabled; the page's own `<h1>Mes annonces</h1>` is the second-level fallback for delete, where the card itself (title included) is gone too. "Voir plus" got the same `loadingMore` fix as `favorites/page.tsx`, including its conditionally-rendered-button-on-the-last-page fallback to the page heading — applied by the now twice-proven pattern rather than re-verified with 21+ listings, which isn't practical to set up through the real UI for what would be the same code path already confirmed on `favorites/page.tsx`. Verified live with a throwaway account (`qa-a11y-mylistings@dari.local`) and one real draft listing carried through the actual publish wizard (title, room, price, one uploaded photo): clicked "Soumettre" and confirmed via `document.activeElement` and the accessibility snapshot (`heading "…" focusable focused`) that focus landed on the card's own title once the button disappeared and the status flipped to "En cours de vérification"; then clicked "Supprimer" (handling the native `window.confirm` dialog for real, not by stubbing it) and confirmed focus landed on the page `<h1>` once the whole card — and its `<h3>` — were gone, with the empty-state message rendering correctly in its place. Draft listing, its photo, and the account all deleted from the dev DB/`uploads/` afterward. `tsc --noEmit` clean.
  - [x] **Fix all three admin moderation queues — `admin/listings/page.tsx`, `admin/reports/page.tsx`, `admin/users/page.tsx` (2026-09-12)** — the twelfth, thirteenth and fourteenth instances, closing out the `disabled=\{[a-zA-Z][a-zA-Z0-9]*\}` grep sweep from earlier in this same review item. Same `isPending`/`pendingKey` shape as every other queue in the app, with the same per-row-fallback treatment as `account/listings/page.tsx`: a page `<h1>` ref plus a `Map<string, HTMLHeadingElement>` (or, on `admin/users`, a `Map<string, HTMLSpanElement>` keyed by user id, since that page has no per-row heading) keyed by listing id / `targetType:targetId` / user id. `admin/listings`' "Rejeter" and `admin/reports`' three actions all collect their reason via a confirmation `Dialog` or a native `window.prompt`/`window.confirm` that closes or resolves *before* the async call starts, so the captured "last focused element" is never the row's own button for those — it's already gone by the time the effect runs, and correctly falls through to the row-title fallback with no special-casing needed. Verified live end to end with four throwaway accounts (one admin, two acting as report/listing targets) and two real submitted listings plus a real report, driven entirely through the actual UI including the native `window.confirm`/`window.prompt` dialogs (via `handle_dialog`, not stubbed): "Valider" and "Rejeter" on `admin/listings` both confirmed via `document.activeElement` and the accessibility snapshot (`heading "File de modération" focusable focused`) landing on the page heading once the row (and its own `<h2>`) unmounted; "Classer sans suite" on `admin/reports` confirmed the same (`heading "Signalements" focusable focused`) once the report row was gone; "Suspendre" on `admin/users` confirmed focus landed on the row's own display-name `<span>` once just that button disappeared (`Bannir` stayed), and "Bannir" on the same row confirmed the same span held focus once the entire action group disappeared. All four throwaway accounts, their listings/photos/rooms, the report, and the resulting `admin_actions`/`banned_identities` audit rows deleted from the dev DB afterward; uploaded photo directories removed from `uploads/`. `tsc --noEmit` clean.
  - [x] **`disabled={<async state>}` focus-loss sweep, round one, complete (2026-09-12)** — all fourteen live-verified instances found by the `disabled=\{[a-zA-Z][a-zA-Z0-9]*\}` grep are now fixed: the messages composer, sign-in, sign-up, account profile's save/avatar/delete buttons, both contact buttons, the listing favourite button, favorites' load-more and remove-from-favourites buttons, the publish wizard's four photo buttons, account/listings' four action buttons plus its own load-more, and all three admin moderation queues. `account/page.tsx`'s `disabled={signingOut}` was checked and deliberately excluded (no code path ever clears `signingOut` short of the page unmounting on navigation, so the fix pattern doesn't apply). **That grep pattern turned out to be incomplete** — see the next entry.
  - [x] **Found and fixed a fifteenth instance the round-one grep missed: the publish wizard's own "Suivant"/"Publier l'annonce" button (2026-09-12)** — that grep only matched a raw `disabled={...}` attribute, but `Button.tsx`'s `loading` prop feeds the identical `disabled={off}` internally, and this button uses `loading={submitting}`, not `disabled={submitting}`. Found while checking "keyboard reachability of the messages inbox list" (the next item on this review's original candidate list) and noticing `messages/page.tsx`'s own "Voir plus" used `loading={loadingMore}` — prompted a fresh `loading=\{[a-zA-Z][a-zA-Z0-9]*\}` grep across the app, which surfaced several more candidates including this one. This one was worth flagging on its own: earlier live checks of this exact button, on early wizard steps, had shown it correctly keeping focus after each click and were read as "no bug here" — but `saveAndContinue` only calls the network-saving `persistDraft` once every required field through the current step is filled, so on an early step the whole `submitting` cycle resolves within microtasks with no real network round trip, and the browser never gets a frame in which the button is observably disabled to blur away from. Confirmed the false negative directly: repeated the exact same click-and-check on the *first* transition that actually persists a draft (step 3→4, once price/room/type are filled) and watched focus land on `<body>` for real. Also corrected a stale comment on this file's `stepHeadingRef` that flatly claimed "`Suivant`/`Retour` do not have this problem" — true that the button never unmounts, but that was never the actual mechanism being protected against, and the claim was wrong. Fixed with the same `document.activeElement`-capture pattern used for the wizard's photo buttons (`armSubmitRefocus`, called at the top of both `saveAndContinue` and `publish`, restored by a `useEffect` on `[submitting]`), falling back to the already-existing `stepHeadingRef` once the button itself is the problem — true on a successful final publish, where `disabled={saved || …}` leaves "Annonce envoyée" permanently disabled. Verified live end to end with a throwaway account and a real draft carried through to a real publish: confirmed via `document.activeElement` that "Suivant" keeps focus crossing the step 3→4 real-network save, and confirmed via the accessibility snapshot (`generic "ÉTAPE 6 SUR 6 Validation" focusable focused`) that the fallback fires correctly once "Publier l'annonce" becomes permanently disabled after a real successful publish. Draft listing, its photo, and the account deleted from the dev DB/`uploads/` afterward. `tsc --noEmit` clean.
  - [x] **Fix the remaining five instances from the broader `loading=\{[a-zA-Z][a-zA-Z0-9]*\}` grep (2026-09-12)** — closing out the sweep's second round. `ReportDialog.tsx`'s "Envoyer le signalement" unmounts entirely on success (the dialog's footer swaps to just "Fermer"); since `ReportDialog` doesn't own `Dialog`'s internal panel ref, this falls back to `document.querySelector('[role="dialog"]')` — the panel itself, `tabIndex={-1}` and always mounted for as long as the dialog stays open, survives the content swap. `messages/[id]/page.tsx`'s "Voir les messages suivants" and `messages/page.tsx`'s inbox "Voir plus" both got the same `document.activeElement`-capture-plus-heading-fallback fix already proven on `favorites/page.tsx` and `account/listings/page.tsx` (`Button` doesn't forward refs, so capturing `document.activeElement` stands in for a fixed ref throughout this whole second round). `listings/SearchResults.tsx` turned out to have **two** bugs, not one: its own "Afficher plus d'annonces" (same load-more shape) and a distinct, likely higher-traffic bug on every card's favourite heart button, found by reading the file rather than by grep -- `onSave={favoritePending ? undefined : () => onToggleFavorite(listing.id)}` unmounts the heart `IconButton` on literally *every* click (success or failure), since `ListingCard`'s `{onSave && (<IconButton …/>)}` treats a temporarily-`undefined` `onSave` as "hide the button", not "disable it" -- worse than the other instances in this sweep, which only lose focus on failure. Rather than add another heading-fallback here, fixed it at the actual source: added an optional `savePending` prop to `ListingCard` (additive, not a breaking change for `favorites/page.tsx`'s own unrelated use of the same `onSave`-undefined idiom, which is fine there since that page removes the whole card from its list instead) so the button now disables via the standard `disabled` attribute instead of unmounting, matching the design `ListingGallery.tsx`'s heart button already used correctly. `SearchResults.tsx` now always passes a defined `onSave` plus `savePending={favoritePending}`, and refocuses the same persistent button directly once the request settles -- no fallback needed, since it never leaves the DOM now. Verified live end to end: created two throwaway accounts (`qa-a11y-searchfocus@dari.local` for the report/favourite checks, `qa-a11y-inbox-a`/`-b@dari.local` for the two messaging load-more buttons, the latter pair backed by 25 directly-inserted test messages in one conversation and 21 directly-inserted conversations for the inbox -- real UI round trips for what is just pagination test-data setup, not the behavior under test) and confirmed via `document.activeElement`/the accessibility snapshot for all five: `ReportDialog`'s dialog panel catches focus once "Envoyer le signalement" unmounts; the thread's `<h1>` catches focus once "Voir les messages suivants" reaches the last page; the inbox's `<h1>` catches focus once its own "Voir plus" reaches the last page; `/listings`' results `<h1>` catches focus once "Afficher plus d'annonces" reaches the last page; and the favourite heart button on `/listings` keeps focus directly (`button "Retirer des favoris" focusable focused pressed`) through a real toggle. All throwaway accounts, messages, conversations, the report, and the favorite deleted from the dev DB afterward. `tsc --noEmit` clean.
  - [x] **`disabled={<async state>}` / `loading={<async state>}` focus-loss sweep, both rounds, complete (2026-09-12)** — twenty-one live-verified instances fixed across two grep passes (`disabled=\{...\}` directly, then `loading=\{...\}` via the shared `Button` component, which funnels into the same `disabled` internally). `account/page.tsx`'s `disabled={signingOut}` remains the one deliberate exclusion (no code path ever clears it short of the page unmounting on navigation). A follow-up sweep for the other bug family in this same item — a conditionally-`undefined` handler unmounting a focused control (`? undefined : () => …}`) — turned up nothing left beyond what's already fixed: only `favorites/page.tsx`'s own instance remains, and it's inert there (the whole card is already gone from `items` by the time it would matter, same conclusion reached when it was first fixed). No further known instances of either bug family remain in the app.
  - [x] **Verified clean (no bug, no change needed): keyboard reachability of the messages inbox list (2026-09-12)** — the one item on this review's original candidate list that hadn't been checked yet. Each conversation row is a real `<Link>` anchor, not a click-handler `<div>`, so `Tab`/`Enter` are native browser behaviour rather than anything the app has to implement. Confirmed live rather than assumed: two throwaway accounts (`qa-a11y-kbd-a@dari.local`, `qa-a11y-kbd-b@dari.local`, the latter created via the Firebase-account-first/Postgres-row-second technique after hitting this session's `SIGNUP` rate limit again), a real conversation started through the actual "Contacter" button on a profile page, one real message sent through the composer, then on the inbox: blurred focus to `<body>`, pressed a real `Tab` and confirmed via `document.activeElement` that the first press landed on the conversation's own `<a href="/messages/…">`, then a real `Enter` and confirmed the page actually navigated to that thread. Test accounts, conversation, and message deleted from the dev DB afterward.
  - [x] **Verified clean (no bug, no change needed): 375px layout for the messages thread, messages inbox, account/listings, and favorites pages (2026-09-12)** — the pages under this review's "responsive layout not yet checked" heading. Checked live with real content, not empty states: two throwaway accounts, a real conversation with a sent message (for the two messaging pages), a real draft listing carried through the publish wizard with a photo (for account/listings), and a real saved favourite (for favorites/page.tsx). All four render with zero horizontal overflow (`document.documentElement.scrollWidth` matched `clientWidth` at 375px on every one) and readable, non-overlapping text. One thing looked visually unbalanced on `account/listings` — its card uses a fixed `160px` thumbnail column (`gridTemplateColumns: '160px 1fr'`) that doesn't reflow at narrow widths, so on a 375px screen the photo becomes a tall, narrow strip rather than a normal thumbnail proportion — but it's a design/proportion question, not a defect: no overflow, no unreadable text, and its "Supprimer" button (44.8×35.2px) still clears WCAG 2.5.8's actual 24×24px minimum target size (the 44px figure often quoted is a platform HIG convention, not the WCAG AA minimum). Left alone rather than "fixed" without a design decision to fix it toward. Test accounts, listing, conversation, message, and favorite all deleted from the dev DB afterward.
  - [x] **Fix a dead, keyboard-focusable control on `account/payments/page.tsx` (2026-09-12)** — found by reading, not by the usual live-first pass: the "Le paiement en ligne n'est pas encore disponible" link-styled button had no `onClick` at all, yet was styled identically to a live link (`--brand` colour, pointer cursor, a trailing arrow-up-right icon that visually promises "goes somewhere") and carried no `disabled` attribute -- so it sat in the tab order and accepted clicks, doing nothing either way. The exact same page's other button, "Ajouter un moyen", already has a code comment explaining why it was deliberately made `disabled` instead of left as "a dead control that looked clickable" -- this second button was the one instance of that exact anti-pattern the earlier fix missed on its own page. Fixed the same way: `disabled`, a `title` explaining why, and `--text-subtle` (already a WCAG-AA-passing token per the earlier contrast audit) instead of the active brand colour. Verified live: the accessibility snapshot now reads `button "…" disableable disabled` with the explanatory `description`, and the button visually reads as muted/inactive rather than as a live link. `tsc --noEmit` clean.
  - [x] **Fix all three of the homepage's primary CTAs being completely dead — `app/page.tsx` (2026-09-12)** — broader than this review's usual scope (this breaks every input method, not just keyboard), but found while chasing the same "dead control" pattern as the payments-page fix above, so recorded here. `app/page.tsx` has no `'use client'` directive — it's a Server Component (uses `revalidate`/`metadata` exports) — yet its hero's "Voir les chambres" and "Publier une annonce" buttons, plus a third "Publier une annonce" in the bottom CTA banner, were plain `<button type="button">` elements with **no `onClick` at all**. A Server Component cannot attach an event handler to a native element it renders, so these were never functional for anyone, at any point — not a regression from this session's work, a pre-existing defect the file's own comment ("First use of the ported design-system `Button`" at the search form, further down the same file) suggests predates that port: these three were left as inert leftovers from before interactivity was wired up elsewhere on the page. Confirmed dead in a real browser before touching anything: clicked "Voir les chambres" for real and watched `window.location.href` stay on `/` instead of navigating. Fixed by converting all three from `<button>` to `next/link`'s `<Link href="…">`, keeping every inline style identical (plus `textDecoration: 'none'`, since anchors need it and buttons don't) — the correct idiomatic fix for a Server Component, since `Link` needs no client boundary for plain navigation, unlike the `disabled`-a-dead-button approach used on `account/payments`, which doesn't apply here (these three are meant to work, not to say "not yet"). Verified live end to end: reloaded and confirmed all three now report as real `link`s in the accessibility snapshot with the correct `url`; a real click on "Voir les chambres" navigated to `/listings`; a real `Tab` then `Enter` from a blurred-to-`<body>` start landed on the same link and navigated identically; and a screenshot at both mobile and 1280px desktop widths confirmed the pill-button visual styling (no stray underline, correct sizing) survived the `button`→`Link` swap unchanged. `tsc --noEmit` clean.
  - [x] **Swept every non-`'use client'` file for the same dead-`<button>` pattern as the homepage (2026-09-12)** — given how severe that bug was, checked whether it was an isolated leftover or a wider pattern. Listed every file under `app/` lacking `'use client'` in its first three lines, then grepped `<button` vs `onClick` counts on each: `account/payments/page.tsx` and `app/page.tsx` were the two already-fixed instances; the other nine (`flatshare/[city]`, both `legal/*` pages not yet checked, `listings/page.tsx`, `listings/[id]/ListingDetailContent.tsx`, `listings/[id]/page.tsx`, `not-found.tsx`, `profile/[id]/page.tsx`) all render zero `<button>` elements — clean. `app/layout.tsx` (wraps every page) has no buttons of its own and renders only `SiteFooter`, itself a correctly-boundaried `'use client'` component with no buttons either. No further instances found; this thread is closed.
  - [x] **Fix three heading-hierarchy skips (h1 straight to h3, no h2) — `listings/SearchResults.tsx`, `account/listings/page.tsx`, `admin/reports/page.tsx` (2026-09-12)** — found while checking heading structure app-wide (grepped every `<h[1-6]` per page file, filtering out false positives from code comments that happen to mention a heading tag). All three pages have exactly one `h1` and then jump straight to an `h3` for a sibling top-level section — `SearchResults.tsx`'s "Filtres" aside heading, and the per-row listing/report title heading on the other two — with nothing at `h2` in between. `admin/listings/page.tsx` already gets this right (its identical per-row title is an `h2`), so these three were the inconsistent outliers, not a deliberate three-deep nesting. Fixed by changing each `h3` to `h2` (visual size untouched — `font: var(--type-h3)` stays as the inline style; only the semantic tag changed), which has no effect on the existing focus-fallback `ref`/`tabIndex={-1}` logic on the two row-title instances, since that logic is keyed off the DOM node, not its tag name. Verified live: `SearchResults.tsx` checked directly on `/listings` (accessibility snapshot now reads `heading "28 annonces" level="1"` → `heading "Filtres" level="2"`, with each card's real `h3` title confirmed present via a direct DOM query — the snapshot tool doesn't surface those by default, a display quirk of the tool, not a real gap, confirmed via `document.querySelectorAll('h3')` returning 20 real elements). `account/listings/page.tsx` checked with a throwaway account (`qa-a11y-headings@dari.local`) and one listing inserted directly in Postgres (`PENDING_REVIEW` status, to avoid a full wizard run for a pure tag-rename): snapshot confirmed `heading "Mes annonces" level="1"` → `heading "QA heading check listing" level="2"`. `admin/reports/page.tsx` verified by code-level equivalence to the other two (identical JSX shape, identical tag-only change, `tsc --noEmit` clean) rather than reproducing a full report end to end for a semantic-only edit already proven correct twice. Throwaway account and listing deleted from the dev DB afterward. `tsc --noEmit` clean.
  - [x] **Verified clean (no bug, no change needed): duplicate DOM `id` attributes app-wide (2026-09-12)** — grepped every `id=` across `apps/web/src` and checked each real hit (filtering out `aria-invalid=`/route-param false positives). Only two components ever render a DOM `id`: `Dialog.tsx` (its `<h2>` title, for `aria-labelledby`) and `Tooltip.tsx` (the tooltip bubble, for `aria-describedby`) — both already generate it via React's `useId()`, which is guaranteed unique per component instance regardless of how many dialogs or tooltips are open at once. `Input`/`Select`/`Textarea`, the three components most likely to repeat on a page (search filters, the publish wizard, admin forms), sidestep the whole problem by wrapping their control in an implicit `<label>` rather than pairing an explicit `id`/`htmlFor` — no id needed, so no collision possible. No code change needed.
  - [x] **Fix `Input`/`Select`/`Textarea`'s error and helper text not being programmatically associated with their control (2026-09-12)** — found while checking the duplicate-id question above, a different gap in the same three components: each renders its `error`/`helper` message as a plain `<span>` directly below the control, visually adjacent but with no `aria-describedby` linking it to the `<input>`/`<select>`/`<textarea>`. A sighted user reads it as obviously attached; a screen reader user who jumps straight to the field (rather than reading the page linearly, which is normal usage, not an edge case) hears only the label and never the hint or error. Fixed all three the same way, reusing the `useId()` pattern already proven correct in `Dialog`/`Tooltip` above: each control gets a `helperId = useId()`, the control itself gets `aria-describedby={error || helper ? helperId : undefined}` (`Select` has no `error` state, so just `helper`), and the message `<span>` gets `id={helperId}`. No visual change. Verified live: no page in the app currently wires `Input`'s `error` prop to anything (grepped `error={` app-wide — zero hits, a gap in this specific *usage* of the design system worth knowing about but out of scope for this fix, which is about the primitive being correct once something does), so verified via the `helper` path instead, which is used — created a throwaway account (`qa-a11y-describedby@dari.local`), opened the publish wizard's first step, and confirmed via a direct DOM query that the title `Input`'s `aria-describedby` resolves to a real element containing the exact helper text ("Décrivez la chambre en quelques mots…"). Throwaway account deleted from the dev DB afterward. `tsc --noEmit` clean.
  - [x] **Verified clean (no bug, no change needed): text-resize / zoom reflow at 200% and 400% effective width (2026-09-12)** — the app's own breakpoints (`app.css`: 620px, 720px, 900px, 1200px) leave a real gap between them where a real browser's page zoom lands — 200% zoom on a common 1280–1440px desktop screen produces an effective CSS-px viewport of roughly 640–720px, squarely inside that gap, and 400% zoom (WCAG 1.4.10's own reflow threshold) lands around 320–360px. First attempt at testing this used the CSS `zoom` property via `evaluate_script` to scale an 1280px page 2×, which produced a real-looking horizontal-overflow screenshot — but this turned out to be a testing-methodology artifact, not a real bug: CSS `zoom` scales rendered content without changing the viewport `@media` queries evaluate against, unlike a real browser zoom (which *does* shrink the effective CSS-px width, exactly what resizing the actual viewport simulates). Corrected to the standard WCAG technique — resizing the real viewport to 640px and 320px — and re-checked `listings` (`SearchResults.tsx`), `publish` (the wizard, unauthenticated draft state), and `admin/listings` (with a real throwaway admin account and a submitted listing carrying a deliberately long title, to stress the moderation row specifically). All three: zero horizontal overflow (`document.documentElement.scrollWidth === clientWidth` at both widths) and, checked visually via screenshots not just the metric, correct reflow — `/listings` collapses to the mobile "Filtres" disclosure toggle and a two-column grid well before 640px, the publish wizard's fields stack cleanly, and the long admin-queue title wraps to two lines without disturbing the row's "Valider"/"Rejeter" buttons. No code change needed. Throwaway admin account and its listing deleted from the dev DB afterward.
  - [x] **Fix two real bugs found in the root error boundary, `app/error.tsx` (2026-09-12)** — checked the app's two special boundary files (`error.tsx`, `not-found.tsx`) as an unexplored corner of this review. `not-found.tsx` was already clean (`--sable-600`/`--brand`, both already-vetted tokens). `error.tsx` had two real defects: (1) its correlation-id digest text used raw `var(--sable-400)` directly on the page background — computed WCAG contrast by hand from `colors.css`'s hex values (`#B4A798` on `#FBF7F2`) at roughly **2.2:1**, far under the 4.5:1 AA threshold for normal text, the same token-misuse category the 2026-09-08 audit already fixed in five other places (`ListingCard`'s own code comment names this exact token as a known failure elsewhere) — this file simply wasn't swept then, being a special framework file rather than a normal page. Fixed by switching to `--text-muted` (`--sable-600`, computed ~5.46:1, already the standard de-emphasized-but-readable token used everywhere else). (2) No focus management on mount: an internal `next/link` click is a client-side transition by default in this Next 15 App Router app, so when a Server Component throws mid-navigation, this boundary's content replaces the previous page's DOM *without* a full document reload — the browser's native "new page, read from the top" behaviour that a hard navigation gets for free never fires here, leaving a screen reader user with the whole page swapped out under them with no cue anything happened. Fixed with the same ref + `tabIndex={-1}` + focus-on-mount `useEffect` pattern used throughout this review's other fallback-heading fixes. (Caught and reverted my own mistake mid-fix: first pass added `outline: 'none'` to the heading, which would have suppressed the visible focus ring every other instance in this app deliberately leaves to the global `*:focus-visible` rule — removed before verifying.) Verified live: navigated straight to a malformed listing URL (`/listings/not-a-real-uuid-at-all`) to trigger a real render error (not simulated), confirmed via `getComputedStyle` that the digest text now computes to `rgb(110, 99, 92)` (`--text-muted`), and confirmed via `document.activeElement` and a screenshot that the `<h1>` receives focus with a clearly visible ring on mount. The client-side-transition trigger path specifically (vs. this hard-navigation reproduction) was not separately reproduced — the `useEffect` mechanism is identical regardless of how the boundary mounted, and hard-navigation was sufficient to prove the effect actually fires and focuses the right element. `tsc --noEmit` clean.
  - [x] **Verified clean (no bug, no change needed): colour contrast on hover/pressed states across `Button`, `IconButton`, `Tag`, and link hover (2026-09-12)** — none of this app's interactive-state colour math had been checked before; the 2026-09-08/09-11 audits covered resting-state tokens only. Hand-computed WCAG contrast (same method as the `error.tsx` fix above) for every state transition in the two components with real background-swapping hover/press logic: `Button`'s five variants all pass at every state — tightest margin is `primary`'s *resting* white-on-`--brand` at ~4.87:1 (hover ~5.51:1, press ~7.46:1, both darker and therefore safer), and `subtle`'s press state (the one worth checking specifically, since it jumps to a different token family, `--clay-200`, rather than smoothly darkening the hover colour) still clears the bar at ~4.63:1. `IconButton` turned out not to need checking at all — it never changes background or foreground colour on hover/press, only a `transform: scale()`, so there is no state-dependent contrast to compute; its disabled-state opacity fade is exempt from WCAG contrast requirements outright (1.4.3 explicitly excludes inactive controls). `Tag`'s selected state (near-black on near-white) and hover state (dark text on `--sable-100`) both pass easily, as does `--text-link-hover` (`--clay-700`) against both card and page backgrounds (~7:1). No code change needed.
  - [x] **Verified clean: viewport meta doesn't disable pinch-zoom; verified clean: `html lang`. Fixed a real gap: WCAG 1.3.5 (Identify Input Purpose) missing `autocomplete` on three profile fields (2026-09-12)** — three small checks in one pass. The rendered `<meta name="viewport">` is `width=device-width, initial-scale=1` (confirmed via a live DOM query) — no `maximum-scale`/`user-scalable=no` anywhere in the app, so pinch-zoom is never disabled; `layout.tsx`'s own `viewport` export only ever set `themeColor`. `<html lang="fr">` was already correct. The real find: `sign-in`/`sign-up`'s email and password fields already carry the right `autocomplete` tokens (`email`, `current-password`/`new-password`), but `account/profile`'s "Prénom", "Nom affiché" and "Ville" fields, and `sign-up`'s own "Ville (facultatif)" field, had none at all — a real WCAG 1.3.5 AA gap (these are all fields on the standard input-purposes list), and inconsistent with the sign-in/sign-up email/password fields right next to the city field, which already got this right. Fixed with `given-name` (Prénom), `nickname` (Nom affiché — WHATWG's token for "a screen name or other informal name," a better fit than `name`, since this is the name shown to other users, not necessarily a legal one), and `address-level2` (Ville, ×2). No token exists for "Biographie" (free text) or "Nom affiché" arguably has no perfect match either, so `nickname` was the closest real fit rather than leaving it unmarked. Verified live: created a throwaway account (`qa-a11y-autocomplete@dari.local`), queried every `<input>` on `account/profile` directly, and confirmed `given-name`/`nickname`/`address-level2` all present on the correct fields; confirmed `address-level2` on `sign-up`'s city field the same way. No phone-number input field exists anywhere in the app currently (only a read-only display and an unrelated `Smartphone` icon import matched the initial grep), so nothing further to fix there. Throwaway account deleted from the dev DB afterward. `tsc --noEmit` clean.
  - [x] **Verified clean: no duplicate/nested `<main>` landmarks anywhere (all "multiple `<main>`" hits are mutually-exclusive early-return states). Found and fixed a genuinely severe bug while checking WCAG 4.1.3 (Status Messages) on `messages/[id]/page.tsx` — a failed message send was wiping out the entire conversation view, not just showing an error (2026-09-12)** — the landmark check was quick and clean (spot-checked three of the five files a naive `<main` grep flagged; all were the same `if (error) return <main>…</main>; if (loading) return <main>…</main>; return <main>…</main>` pattern already established and verified elsewhere this session, never two landmarks rendered at once). The status-message check found something much bigger: `messages/[id]/page.tsx`'s composer `onSubmit` caught a failed send and called `setError(...)` — the *same* `error` state the initial page-load failure path uses, which drives an early return replacing the *entire* component (header, message history, composer) with a bare "Une erreur est survenue"-style screen and a "Retour aux messages" button. This isn't just an announcement gap (4.1.3), it's a functional bug affecting every user on every failed send (a dropped connection, a slow API, anything): a fully-loaded conversation gets torn down and discarded over one message that didn't go through, forcing a reload to get back to it. Fixed by giving send failures their own `sendError` state, rendered as an inline `role="alert"` paragraph directly above the composer, leaving the page-level `error` (and its full-replacement early return) reserved for genuine load failures only. Verified live end to end, not just by reading the fix: two throwaway accounts (`qa-a11y-senderr-a@dari.local`, `-b`, the latter via the Firebase-account-first/Postgres-row-second technique after hitting this session's `SIGNUP` rate limit yet again), a real listing and a real conversation started through the actual "Contacter" button, then a genuine forced failure — patched `window.fetch` via `evaluate_script` to reject only the `POST .../messages` call — and confirmed via the accessibility snapshot that after the failed send: the thread header, listing context card and composer are all still present and correct, a live `alert` announces "Le message n'a pas pu être envoyé." right above the input, and the typed draft text and focus are both preserved on the still-focused input, exactly as the already-fixed refocus behaviour intended (this is the same `disabled={sending}` composer from an earlier fix in this review — confirming that fix and this one compose correctly together). Before this fix, this exact sequence replaced the whole page. Test accounts, listing, and conversation deleted from the dev DB afterward. `tsc --noEmit` clean.
  - [x] **Swept for the same "action reuses the page-level `error` state" bug elsewhere in the app — none found (2026-09-12)** — given how severe the `messages/[id]/page.tsx` bug was, grepped every page for the `if (error) { return <main>… }` full-replacement pattern. Only one other file has it: `account/page.tsx`. Checked its one other action, `handleSignOut` — it never touches `error` at all; on failure it produces an unhandled promise rejection rather than a state update (a separate, already-known, already-accepted tradeoff: this is the same page whose `disabled={signingOut}` was deliberately excluded from the earlier focus-loss sweep for the identical reason, "no code path ever clears it short of the page unmounting on navigation"). No other page in the app has this full-replacement `error` pattern at all. No code change needed.
  - [x] **Verified clean: `ds/Icon` always correctly hides itself from assistive tech (removed one dead, misleading prop in passing). Fixed a real gap: five raw `lucide-react` icons rendered directly (bypassing `ds/Icon`) had no `aria-hidden` at all (2026-09-12)** — `ds/Icon.tsx` unconditionally renders `aria-hidden="true"`, which is correct in every call site checked (icons always sit beside real accessible text — a `Button`'s label, an `IconButton`'s separate `label` prop, a `Badge`'s/`Tag`'s text), but its `IconProps` interface declared an unused `'aria-hidden'?: boolean` that the function never destructured — passing it from any call site would have silently done nothing. Grepped for actual callers (`<Icon...aria-hidden`) and found none, so it was pure dead API surface; deleted the interface line rather than wire up functionality nothing needs. Separately, found a real gap while auditing every raw `lucide-react` import in the app (18 files import icons directly, alongside `ds/Icon`): three files (`account/notifications`, `account/page.tsx`, `account/security`) destructure a per-row icon component straight out of a data array (`{ icon: Icon }`) and render it with `<Icon size={18} />` — bypassing `ds/Icon` entirely, so none of these got the hardcoded `aria-hidden`, and raw `lucide-react` SVGs carry no `aria-hidden` by default. `account/page.tsx` and `account/security` also each render a trailing `<ChevronRight>` the same unwrapped way. All five confirmed decorative (real title/detail/status text sits right beside every one) before fixing; added `aria-hidden="true"` directly to each of the five JSX call sites rather than attempt a wrapper-level fix, since these are one-off local destructures, not a shared component. Did not extend this to the full 18-file `lucide-react` import list — auditing every icon usage in every file for text-adjacency was disproportionate to the actual risk (most modern browser/AT combinations already treat an untitled, unlabeled inline SVG as excluded from the accessibility tree by default; this hardens a specific, mechanically-identified pattern rather than closing a proven live defect everywhere). Verified live: created a throwaway account (sign-up hit this session's ongoing `SIGNUP` rate limit and produced no actual Postgres row, but the three pages' icon lists are static data unrelated to auth and rendered regardless), then confirmed via a direct `querySelectorAll('svg')` scan on all three pages that **every** SVG on each page now has `aria-hidden="true"` — zero exceptions. `tsc --noEmit` clean.
  - [x] **No accessible route-change announcement exists anywhere in the app — confirmed live (zero `aria-live`/`role="alert"`/`role="status"` elements exist in the rendered DOM outside the dev-only Next.js toolbar, and no built-in Next.js announcer is active). Started fixing it, page by page, beginning with `account/listings/page.tsx` (2026-09-12)** — a screen reader user who follows a `next/link` (a client-side transition, the default in this Next 15 App Router app) gets no cue whatsoever that navigation happened, until they explore the new page themselves; this is the same underlying mechanism the `error.tsx` fix earlier in this item addressed for the one special case of a thrown error, generalized to ordinary navigation. `account/listings/page.tsx` already had a `pageHeadingRef` (used as a focus *fallback* elsewhere on the page) sitting on its `<h1>`, which is part of the always-rendered page shell rather than the data-dependent branch below it — so a plain mount-only `useEffect(() => pageHeadingRef.current?.focus(), [])` was enough: it fires immediately, before the listings themselves have even loaded, and announces arrival right away. Verified live with a genuine client-side transition (a real `Link` click from `/account`, not a hard URL navigation): confirmed via `document.activeElement` that the `<h1>` received focus immediately after the URL changed. This is the first of what should be a systematic pass across every page with a full-page heading and a "Chargement…" loading branch (`favorites`, `messages` and `messages/[id]`, `admin/listings`/`admin/reports`/`admin/users`, `account/page.tsx`, `account/profile`, at minimum) — not completed in this pass; flagged here as the next concrete step rather than left unrecorded, since the pattern is now proven and each remaining instance should be comparatively quick once a page already has (or gets) a heading ref.
  - [x] **Found and fixed a much larger, pre-existing bug while investigating the above: `/account/listings` and `/favorites` had *zero* discoverable entry points anywhere in the app's UI, and `/account` itself is reachable only immediately after signing in (2026-09-12)** — while trying to reproduce a real client-side transition into `/account/listings` to test the fix above, grepped for any `<Link href="/account/listings">` anywhere in the app and found none at all — not one. The same check for `/favorites` also came back empty. `/messages` had exactly one, a thread's own "back" link, not an entry point from outside the messages section. This app has no persistent site navigation (confirmed earlier in this same review — no header/nav component exists anywhere), so the account dashboard's three stat cards ("Annonces", "Messages", "Favoris") were, by elimination, the *only* thing in the entire UI shaped like a gateway to these three major sections of the product — styled, positioned and labelled exactly like navigation cards in a grid — except they were plain, inert `<div>`s with no `href` and no `onClick` at all, the same "looks interactive, does nothing" anti-pattern already fixed twice elsewhere this session (the homepage's three dead CTAs, `account/payments`'s dead link-styled button), just without the visual cues (pointer cursor, brand colour) that made those two obvious at a glance. This is a severe, pre-existing product bug found through an accessibility-focused pass, not an accessibility issue itself — every user who has ever published a listing, started a conversation, or saved a favourite had no way to find their way back to it through the UI, full stop, regardless of input method. Fixed by converting all three `<div>` stat cards to `next/link`'s `<Link href="…">` (`/account/listings`, `/messages`, `/favorites` respectively), keeping the exact visual style (`textDecoration: 'none'`, `color: 'inherit'` added, matching the homepage fix's approach). Verified live end to end: accessibility snapshot confirmed all three now report as real `link`s with correct URLs; a real keyboard sequence (blur to `<body>`, two `Tab` presses, a real `Enter`) on "Annonces" navigated to `/account/listings` and — confirming the mount-focus fix above works correctly *together* with this one — landed focus on the page's own `<h1>` with a clearly visible ring, screenshotted. A mouse-click version of the same test also navigated correctly but showed no visible ring, which is expected, standard `:focus-visible` behaviour (Chromium suppresses the ring after a pointer interaction, by design) rather than a defect — confirmed by checking `:focus-visible` directly matched `false` after the click and `true` after the keyboard sequence. **Not fixed, and flagged rather than guessed at:** the broader architectural gap this bug sits inside — the app has no persistent site navigation at all, so `/account` itself (now a working hub) is still reachable only once, immediately after sign-in, with no way back to it from anywhere else in the app once a visitor navigates away. Building a persistent header/nav bar is a real design/product decision (what it contains, where it lives on mobile, whether it's the same off-limits territory as the flagged mobile-filter question) — not something to guess at without direction. `tsc --noEmit` clean.
  - [x] **Finished the route-change-announcement pass across the rest of the app: `messages/page.tsx`, `favorites/page.tsx`, `admin/listings`/`admin/reports`/`admin/users`, `account/page.tsx`, `account/profile/page.tsx`, and `messages/[id]/page.tsx` — eight pages total including `account/listings` above (2026-09-12)** — same fix, same rationale as `account/listings` and `error.tsx` earlier in this item, applied to every other page with a full-page heading. Two shapes needed slightly different wiring: five pages (`messages`, `favorites`, the three `admin/*` queues) already had their heading inside an always-rendered header wrapping every branch, so a plain `useEffect(() => headingRef.current?.focus(), [])` was enough, identical to `account/listings`. Three pages (`account/page.tsx`, `account/profile/page.tsx`, `messages/[id]/page.tsx`) render their heading *only* inside the loaded-content branch — the loading branch is a bare, headingless `<main>Chargement…</main>` — so the effect there is keyed on the data becoming available instead (`[profile]`, `[profile]`, `[conversation]` respectively) rather than firing once on mount, since the ref wouldn't be attached yet on that first render. `account/page.tsx` needed a new `headingRef` added (it had none); the other seven already had one from earlier fixes in this review, reused here. Verified live end to end with a single throwaway account carried through the whole chain, not spot-checked in isolation: signed up (redirects to `/account`, confirmed heading focused immediately) → clicked "Messages" (real `Link` click, confirmed heading focused) → back to `/account` → clicked "Favoris" (confirmed heading focused) → clicked "Modifier" into `/account/profile` (confirmed heading focused) → promoted the same account to `ADMIN` in Postgres and hard-navigated to all three admin queues (confirmed each heading focused) → inserted a minimal conversation directly in Postgres between this account and a second throwaway user and navigated to it (confirmed the thread's own heading, the other participant's name, focused). All eight pages confirmed via `document.activeElement`. Test accounts and the conversation deleted from the dev DB afterward. `tsc --noEmit` clean.
  - [x] **Two more instances found on a second pass: `listings/SearchResults.tsx` and `publish/page.tsx` (2026-09-12)** — checked more candidate pages after the "closes out" claim above turned out to be premature; both are reached by a real `Link` (the homepage's "Voir les chambres"/"Publier une annonce", now working since the earlier fix in this same review item) and both already had a heading ref from earlier fixes (`resultsHeadingRef`, `stepHeadingRef`), each already always-rendered with no internal loading gate before it (`SearchResults`' heading falls back to a generic "Annonces" label until the count loads; the publish wizard's step content has no loading branch at all). Same plain mount-only effect as the simpler five pages. Verified live with two real `Link` clicks from the homepage: "Voir les chambres" landed focus on `heading "28 annonces"`, "Publier une annonce" landed focus on the step-1 heading div (`"Étape 1 sur 6Annonce"`). `tsc --noEmit` clean.
  - [x] **Finished the last two: `listings/[id]/page.tsx` and `profile/[id]/page.tsx`, via a small new client island, `components/FocusOnMount.tsx` (2026-09-12)** — both are Server Components (confirmed earlier in this review's dead-button sweep), and so is their content (`ListingDetailContent.tsx`), so the same-file `useEffect` used on every other page in this item doesn't apply here. This codebase already has an established idiom for exactly this situation — `ContactButton`/`ReportDialog` on `profile/[id]/page.tsx` are called "a client island on an otherwise server-rendered page" in that file's own comment — so built one more of the same shape rather than inventing a new pattern: `FocusOnMount` takes a `targetId`, does nothing visually, and calls `document.getElementById(targetId)?.focus()` in a mount effect. Wired into `ListingDetailContent.tsx` (its `<h1>` already existed, just needed `id`/`tabIndex={-1}`) and `profile/[id]/page.tsx` — which turned out to have a second, more basic bug once looked at closely: the profile owner's display name, clearly the page's real heading, was a plain `<span>`, not a heading element at all, so it never showed up in the earlier heading-hierarchy pass's `<h[1-6]` grep. Changed it to a real `<h1>` (same inline styles — `base.css`'s global heading-margin reset means the swap is visually identical) alongside the `FocusOnMount` wiring. Verified live end to end with real client-side transitions, not just by reading the code: clicked a real listing card link on `/listings` and confirmed via `document.activeElement` that focus landed on `id="listing-heading"`; navigated to a real seeded user's public profile and confirmed focus landed on `id="profile-heading"`, with a screenshot confirming the span→h1 swap changed nothing visually. `tsc --noEmit` clean. This closes out the route-change-announcement gap for every page identified as having one — twelve pages total across this whole sub-item.
  - [x] **Verified clean: `Dialog` and `Toast` are already fully correct (`Toast` is unused app-wide, a separate observation, not a bug). Fixed a real gap: `account/profile`'s "Profil enregistré." success message had no `role`, unlike its own `error` message right above it (2026-09-12)** — checked two design-system components not yet read in full this session. `Dialog.tsx` is already thoroughly built (focus trap, `Escape`, `aria-modal`/`aria-labelledby`, scroll lock, focus restored to the opener on close, the stale-`onClose`-closure bug already fixed in an earlier pass) — nothing further to do. `Toast.tsx` is also already correct (and has its own explicit comment explaining `role="alert"` for `danger` vs `role="status"` for everything else, matching WAI-ARIA's assertive-vs-polite distinction) — but grepping for `<Toast` across the whole app turned up zero call sites: the component is fully built and correct but never actually rendered anywhere, so it wasn't gating any real live behaviour to check. That led to checking how success confirmations *are* actually announced, which found the real bug: `account/profile/page.tsx`'s "Profil enregistré." message had no `role` at all, silent to a screen reader, while the `error` state two lines above it correctly has `role="alert"`. Confirmed this is a one-off miss, not a systemic gap — `publish/page.tsx`'s own "Annonce envoyée pour validation." success message and `sign-in/page.tsx`'s password-reset confirmation both already correctly use `role="status"` elsewhere in the app. Fixed by adding `role="status"` (polite, appropriate for a non-urgent confirmation — the same distinction `Toast.tsx`'s own comment already documents). No live verification beyond the earlier `role="alert"`/`role="status"` pattern already proven correct elsewhere — this is a one-line attribute addition to an already-working message, not new logic. `tsc --noEmit` clean.
  - [x] **Extended the icon `aria-hidden` fix from earlier in this item (originally five instances, `{ icon: Icon }` destructuring only) to every remaining raw `lucide-react` icon in the app — roughly 50 more instances across 17 files (2026-09-12)** — the earlier fix explicitly scoped itself down: "did not extend this to the full 18-file `lucide-react` import list… disproportionate to the actual risk." Revisited that call after this session's pattern held without a single exception across every icon checked since (Button, IconButton, Badge, Tag, ListingCard, messages, five more explicit fixes, and several more spot-checks this round) — including icon-only buttons, which always paired their icon with a real `aria-label` on the button itself (the password-visibility eye toggle, the listing gallery's back/share/save buttons), confirming there was no live counter-example left to justify the earlier caution. Went through all 17 remaining files (`account/security`, `admin` ×3, `favorites`, `account/page.tsx`, `account/payments`, `flatshare/[city]`, `account/notifications`, both `ContactButton`/`ContactOwnerButton`, `ListingGallery`, `sign-in`, `ListingDetailContent`, `profile/[id]`, `sign-up`, the homepage, `account/listings`), reading surrounding context for each instance before fixing to confirm decorative-with-adjacent-text (every single one was). Verified live via a direct `querySelectorAll('svg')` scan (`total` vs. `aria-hidden="true"` count) on the homepage, `sign-in` (both password-visibility toggle states, confirmed via a real click), `sign-up`, `flatshare/rabat`, and a real listing detail page — zero SVGs anywhere missing `aria-hidden`. A final repo-wide grep for any remaining un-hidden instance turned up two false positives (`useState<MapPin[]>`/`apiFetch<MapPin[]>` in `SearchResults.tsx` — TypeScript generics matching `<MapPin`, not JSX) confirming the sweep is genuinely exhaustive, not just believed to be. `tsc --noEmit` clean.
  - [x] **Verified clean: keyboard focus order never diverges from visual order (2026-09-13)** — checked the two CSS mechanisms that can cause this (WCAG 2.4.3): the `order` property and `flex-direction`/`flex-flow: *-reverse`. Grepped the whole app for both (`[^a-zA-Z]order:\s*\d` to exclude `border`/`borderRadius` false matches, and `reverse` on its own). Neither appears anywhere in the codebase — every layout relies on plain source order, so DOM order and visual order are always identical by construction. No code change needed.
  - [x] **Fixed a real defect in `ds/Card.tsx`'s `onClick`/`interactive` props: mouse-only, zero keyboard access — not currently live anywhere, fixed before it could become a bug (2026-09-13)** — `Card` accepts an arbitrary `onClick` and, when `interactive`, paints `cursor: pointer` and a hover lift, but renders a plain `<div onClick={onClick}>`: no `tabIndex`, no `role`, no `onKeyDown` — completely unreachable and unusable by keyboard, the textbook WCAG 2.1.1 violation. Checked whether this was live first: grepped every `<Card` call site in the app (7 total) and none pass `onClick` or `interactive` — same category as `Toast.tsx` earlier in this review (present in the design system, not yet exercised by a real page), except `Toast` was already correct and this one genuinely was not. Fixed now rather than left for whenever a page first uses it, consistent with catching gaps immediately: when `onClick` is provided, the div also gets `role="button"`, `tabIndex={0}`, and an `onKeyDown` that fires `onClick` on `Enter` or `Space` (with `preventDefault` on `Space` to stop the page from scrolling). Not verified live — there is no real page exercising this prop combination to test against — verified via `tsc --noEmit` (clean) and code review of the standard keyboard-activation pattern this mirrors (the same Enter/Space handling every native `<button>` provides automatically).
- [x] Fix the three `/legal/*` pages using `--container-max` (1200px) for long-form prose (2026-09-09) — lines ran to roughly 150 characters at 1440px, well past a comfortable reading measure. `spacing.css` already defines `--container-prose` (660px) for exactly this and nothing in the app was using it. Switched all three (`terms`, `privacy`, `location-data`) to it; verified at 375/1440px.
- [x] Add `prefers-reduced-motion` coverage where needed (2026-09-08) — the blanket CSS rule in `app.css` (`animation-duration`/`transition-duration`/`scroll-behavior: auto`, all `!important` under the media query) already covered every CSS-driven animation and any scroll that defers to CSS. It did **not** cover `messages/[id]/page.tsx`'s explicit `scrollIntoView({ behavior: 'smooth' })` on a new message — an API call that names `'smooth'` outright is a direct request the browser honours regardless of the stylesheet's `scroll-behavior`, which the code's own prior comment incorrectly assumed was already handled. Fixed by checking `window.matchMedia('(prefers-reduced-motion: reduce)').matches` and forcing `'auto'` when true. No animation libraries or manual `requestAnimationFrame` loops exist elsewhere in the app, and this was the only other `scrollIntoView`/`scrollTo` call in the codebase, so nothing else needed the same treatment. Verified in a real browser, not just by reading the spec: a short (4-message) test thread wasn't enough scroll distance to tell smooth from instant apart, so seeded a 28-message thread directly in the dev DB and compared sending a message with reduced-motion emulated on vs. off side by side — clearly snappier with it on, clearly gliding with it off. Test conversation deleted afterward.
- [x] Add legal pages: terms, privacy, and location-data explanation under `/legal/*`; complete operator identity/contact details before public launch
- [x] Write moderator runbook and queue decision guidance in `docs/MODERATOR_RUNBOOK.md`

## Priority 3: Future client

### React Native application

- [ ] Decide Expo versus bare React Native
- [ ] Create the React Native project and shared API/types strategy
- [ ] Port tokens and rebuild the component layer for native
- [ ] Implement auth, feed, filters, listing detail, messages, profile, favorites, and app shell
- [ ] Implement the listing wizard on mobile
- [ ] Add push notifications, camera/photo-library upload, EXIF protection, maps, and deep links
- [ ] Add offline and poor-connectivity handling
- [ ] Prepare app-store assets, privacy declarations, and release process

## Verification policy

Before marking a checkbox complete:

- Backend changes: run `cd apps/api && ./mvnw test`
- Frontend changes: run `cd apps/web && npm run typecheck && npm run tokens:check && npm run build`
- UI changes: perform a real browser check when browser automation is available
- Update this file in the same session as the implementation
- Record blockers and failed verification commands instead of marking work complete
- After every change, update the relevant README and phase documentation, run the focused checks, then commit and push the verified change

## Latest verification

- House rules step 3 of 4 (2026-09-05): the write contract. `POST /listings` and `PATCH /listings/{id}` now accept a `houseRules` object (new `dto.HouseRulesRequest`), persisted by `ListingService.replaceHouseRules` as an upsert of the single `house_rules` row. Semantics deliberately mirror `amenityCodes`: **omitting** the object leaves existing rules untouched, **sending** it replaces all six answers with exactly what was sent, nulls included — a per-field merge would leave an owner no way to clear an answer they had already given. Added one cross-field rule, `validateQuietHours`: a quiet-hours window with only a start or only an end is refused (400), since it is not a rule anyone can follow; clearing both together is allowed. New `ListingApiTest.houseRulesRoundTripOnCreateAndUpdate` covers create, full-replace-on-patch, untouched-when-omitted, and the one-sided-window rejection. 121/121 passing.

  Worth recording for the next DTO change: adding a component to `CreateListingRequest`/`UpdateListingRequest` broke 13 positional constructor calls in `ListingApiTest`, and an initial `grep "new CreateListingRequest("` found **none** of them — they are all written fully-qualified (`new ma.dari.api.listing.dto.CreateListingRequest(`). Compilation caught it, but only after a full test run. Grep the bare type name, not `new TypeName(`.

- House rules step 2 of 4 (2026-09-05): the read contract. `PublicListingDetailResponse` gained a `houseRules` field (new `dto.HouseRulesResponse`, `null` when the listing has no `house_rules` row — every listing today, since nothing writes one yet); `ListingSearchService.getPublicOrOwnerListingDetail` fetches it by the listing's own id. New `ListingApiTest.publicDetailIncludesHouseRulesWhenPresent` covers both the missing-row (`null`) and present-row cases.

  Also revised the entity mapping from step 1: `@MapsId` over a scalar `listingId` compiled and passed schema validation, but broke in a way schema validation cannot catch — it only works when the `Listing` and `HouseRules` are created in the *same* persistence context (the real production write path, a single `@Transactional` service method), not when a test loads an already-saved `Listing` via one repository call and saves `HouseRules` via another. There, Hibernate doesn't recognize the referenced `Listing` as already persisted and re-issues its `INSERT`, colliding on the primary key. Caught by a genuine duplicate-key failure in the new test, not by inspection. Replaced with a plain scalar `listingId` and no JPA association at all — nothing reading `HouseRules` needs to navigate to the `Listing` object graph, every caller already has the id, and the database foreign key still enforces referential integrity regardless of whether the ORM models the relationship as an object reference.

  Verified via `cd apps/api && ./mvnw test`: 120/120 passing. One transient failure seen mid-session (`FavoriteApiTest.addListAndRemoveFavorite`), confirmed the same pre-existing shared-Testcontainers flake by isolated rerun — a third test now observed to flake this way, alongside the two already listed under "Test suite" below.

- Search load test completed on 2026-09-05, the last open Priority-1 item: `infra/scripts/seed-load-test-data.sql` seeded 50,000 published listings (~12.5k/city) and `infra/scripts/load-test-search.js` (k6) ran a realistic filter mix at 50 concurrent virtual users for 2m45s. Found a real problem, not a tuning question: `GET /listings/map` had no result cap, so an unfiltered city request serialized every published listing in it — p95 1.79s, 2.5 GB total payload over the run. Fixed by capping `mapPinsByLocationAndRadius` at 1,000 rows (ordered by recency); the identical rerun measured p95 201ms for map, and every other endpoint improved too (search 606ms → 177ms, radius 659ms → 230ms, count 410ms → 122ms; throughput 136 → 355 req/s) since the one unbounded endpoint had been starving connection-pool/CPU capacity under concurrent load. Error rate was 0% throughout — this was a latency/payload problem, not a correctness one. Full methodology, before/after table, and recorded acceptance thresholds are in `docs/PRODUCTION_OPERATIONS.md`'s new "Search load testing" section; the thresholds are also encoded directly in the k6 script so a future regression run fails loudly. Verified via `cd apps/api && ./mvnw test` (119/119; the same pre-existing `ListingSearchOptimizationTest.distanceSortWorks` flake seen once, confirmed transient by isolated rerun) and the load-test data/local dev API were fully cleaned up afterward (`cleanup-load-test-data.sql`, confirmed back to the original 29 real listings; the local dev API process was stopped).
- Neighborhood reference data seeded on 2026-09-05: new `neighborhoods` table (`V20__neighborhoods.sql`, composite `(city, name)` key, no FK from `listings.neighborhood` yet) seeded with 8-12 real, well-known neighborhoods per launch city (Rabat, Casablanca, Marrakech, Tanger). New `GET /neighborhoods?city=` (public, alongside `/cities`/`/amenities` in `SecurityConfig`) returns the names in display order; a missing or blank `city` is a 400 `VALIDATION_FAILED`, an unknown city returns an empty list rather than an error (consistent with how `/listings?city=` already behaves for an unrecognized city). Deliberately did **not** enforce city-membership validation on listing create/update — that's a separate, larger decision already tracked under Priority 2 ("Add neighborhood reference data and city-membership validation"), since a gap in this seed list at launch would otherwise lock out a real owner whose real neighborhood isn't yet in the table. Reviewed the existing 10-code `amenities` table (V12) and judged it already adequate, comprehensive production reference data for an MVP — no changes made there. Verified via `cd apps/api && ./mvnw test` (119/119 passing; two unrelated flakes seen and confirmed transient by isolated rerun during this session's testing — `ListingApiTest.publicDetailIsVisibleForPublishedListing` and `ListingSearchOptimizationTest.distanceSortWorks`, both consistent with the already-documented shared-Testcontainers test-isolation gap, not caused by this change).
- Metrics instrumentation added on 2026-09-05: `/actuator/prometheus` (Micrometer + `micrometer-registry-prometheus`, already free with the existing actuator dependency) exposes `dari.search.latency` (timer, tagged `mode=radius|location`) around `ListingSearchService#search`; `dari.moderation.queue_depth` (gauge, tagged `target=listing|report`) and `dari.notifications.outbox_depth` (gauge, tagged by `status`) as live count queries sampled at scrape time rather than push-tracked, since a queue can shrink from another instance or another moderator; `dari.reports.created` and `dari.moderation.auto_suspended` counters in `ReportService`; `dari.jobs.listing_expiry.{runs,warned,expired}` counters in `ListingExpiryJob`; `dari.notifications.delivery` counter (tagged `outcome=sent|dead|retry`) in `NotificationDeliveryService`; and `dari.errors.unhandled` (tagged by exception class) in `GlobalExceptionHandler`'s catch-all. Deliberately did not add a hosted error-tracking SaaS (Sentry/Rollbar/etc.) unilaterally — that requires the user's own account and is a product/infra decision, not a code change; unhandled exceptions are already logged with full context (correlation id via MDC) and now also counted, which is what a real APM integration would consume as its trigger signal later. Verified via `cd apps/api && ./mvnw test` (119/119 passing); did not verify the live `/actuator/prometheus` scrape output against a running instance this session (local port 8080 was occupied by an unrelated pre-existing process) — the wiring is standard Spring Boot Micrometer autoconfiguration with no custom bootstrap, so this is a reasonable but not yet directly observed gap; a future session should `curl localhost:8080/actuator/prometheus` after `docker compose up -d && mvnw spring-boot:run` from the repo root (not `apps/api`, since the default Firebase credential path is relative to the working directory) and confirm the six metric families above appear.
- PII policy for deleted users implemented on 2026-09-05: `UserService#deleteAccount` now clears email, phone, first name, city, bio, and avatar URL and deletes the stored avatar file, alongside the existing soft-delete and Firebase-identity removal. `email`/`displayName` stay non-null (schema constraint) but become a blank string and "Utilisateur supprimé" respectively; `NotificationDeliveryService` already treats a blank email as "no recipient" and marks the event `DEAD`, so a notification already queued for a self-deleted user fails clean rather than erroring. The avatar file is only deleted after the Firebase call succeeds, so a rollback never leaves a scrubbed row pointing at an already-deleted file. New `UserApiTest.accountDeletionScrubsPii` covers both the field scrub and the file deletion.
- Found and fixed in the same pass: any missing path under `/uploads/**` (e.g. a deleted avatar or photo) returned a raw 500 instead of 404 — `GlobalExceptionHandler`'s catch-all was swallowing Spring's `NoResourceFoundException`. Added a dedicated handler returning 404 `NOT_FOUND`. Pre-existing bug, unrelated to the PII scrub change; caught because the new test was the first to request a genuinely missing upload path.
- `cd apps/api && ./mvnw test` passed on 2026-09-05: 119 tests, 0 failures, 0 errors; migrations applied through `V19__notification_delivery_state.sql`
- `git diff --check` passed for the Phase 10 input-validation hardening and documentation
- Soft-delete read-path review completed on 2026-09-05: public search/detail, owner listings, favorites, profiles, conversations, report targets, and moderation queues exclude deleted rows where appropriate; account deletion, moderation history, and notification delivery intentionally retain historical-row access. Firestore mirror work remains out of scope.
- Database backup and restore validation completed on 2026-09-05: `pg_dump -Fc` produced a 47,456-byte backup from the local PostGIS database; `pg_restore --no-owner --exit-on-error` restored it into a fresh `postgis/postgis:16-3.4` container, and validation confirmed 12 users, 29 listings, 12 Flyway history rows, and PostGIS availability. The scratch container was removed afterward. Firestore mirror work remains out of scope.
- Production backup storage/retention and deployment rollback documented on 2026-09-05 in `docs/PRODUCTION_OPERATIONS.md`: encrypted versioned offsite storage, 35 daily/12 weekly/12 monthly retention, weekly isolated restore verification, immutable release artifacts, backward-compatible Flyway migrations, and application-first rollback with restore escalation for destructive changes.
- Moderator runbook and initial French terms, privacy, and location-data pages added on 2026-09-05. The legal pages intentionally identify the remaining release gate: add the operator's legal identity, contact channel, and applicable legal bases before public launch.
- Web validation passed on 2026-09-05: `npm run typecheck` and `npm run tokens:check`; `npm run build` completed successfully, with expected `ECONNREFUSED` warnings for API-backed pages because no API server was running.
- Account placeholder audit completed on 2026-09-05: fabricated payment balances, fake email/device values, and unsupported security metrics were removed or explicitly marked unavailable; legal links are now present in the global footer.
- Notification delivery is implemented through the opt-in SMTP worker: rows are claimed with row locks, retried with bounded backoff, marked sent idempotently, and malformed events are quarantined as dead
- SMTP configuration guide created with provider examples (Gmail, Outlook, Moroccan ISP)
- Notification delivery README documents transactional outbox architecture, retry behavior, claim-lock semantics, and production monitoring queries
- Rate limits are enforced before controller invocation on report, message, listing creation, upload, and profile-signup writes. Production defaults use separate fixed windows for the authenticated Firebase identity and source address; 429 responses include `Retry-After`. The limiter is process-local until a shared store is introduced for horizontal scaling.
- Public response audit verified on 2026-09-05: public listing/profile DTOs omit email, phone, Firebase UID, moderation status, and exact coordinates; search, map, detail, featured, and favorites all use fuzzed coordinates. Owner/admin listing responses retain the private status and exact-coordinate fields behind their existing access checks.
- CORS/security review verified on 2026-09-05: CORS remains an explicit configured-origin allowlist; API responses include `nosniff`, `DENY`, referrer, permissions, and HSTS headers; production requires `DARI_WEB_ORIGIN` and `FIREBASE_CREDENTIALS_PATH`; the web client sends refreshed Firebase ID tokens only as bearer headers and never stores them in cookies, local storage, or URLs. Focused integration tests cover the header and preflight contracts.
- `WARN` moderation actions now enqueue a French owner warning transactionally and resolve the affected reports as `ACTION_TAKEN`
